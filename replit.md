# Indian Stock Market Scanner

A comprehensive platform for scanning and analyzing the Indian stock market, providing real-time insights for traders and investors.

## Run & Operate

- **Run (per-artifact)**: Workflows handle this — do not `pnpm dev` at root. Use `restart_workflow "artifacts/api-server: API Server"` etc.
- **Codegen**: `pnpm --filter @workspace/api-spec run codegen` (regenerates `lib/api-client-react/src/generated/*` and `lib/api-zod/src/generated/*` from `lib/api-spec/openapi.yaml`).
- **Refresh NIFTY 500 sector reference**: `pnpm --filter @workspace/scripts run refresh-nifty500-sectors -- --in <nse-industry-weights.csv> --as-of YYYY-MM-DD [--write]` (ingests NSE's published industry weightage CSV, rolls it into app sector buckets as a true partition, validates ~100% sum, emits `NIFTY500_SECTOR_REFERENCE` + as-of into `artifacts/scanner/src/lib/portfolio/benchmark.ts`; dry-run by default, refuses to fabricate when source is missing/invalid).
- **Backfill pre-fix backtest trade times (one-off)**: `pnpm --filter @workspace/scripts run fix-backtest-trade-times [-- --write]` (corrects backtest runs persisted BEFORE the 2026-06-05 timezone fix, whose `modeled` DIRECTIONAL/STRATEGY `entry_at`/`exit_at` + `summary.equityCurve[].t` were stored +05:30 ahead and rendered off-session, e.g. "07:00 pm"). **Decision: backfill, NOT discard** — the error is a deterministic, lossless +05:30 offset, so a −05:30 shift re-derives the true instant while preserving each saved run. Buggy runs are detected structurally (a run with ≥1 `modeled` trade outside 09:15–15:30 IST), so the script is idempotent and never touches correct post-fix runs nor genuine REAL_REPLAY (`modeled=false`, real `triggered_at`) timestamps — some of which legitimately fall off-session. Dry-run by default; verifies 0 off-session `modeled` trades remain after `--write`. Applied to dev DB 2026-06-05; run against prod once after deploy.
- **Fetch Kite candles for the F&O backtester**: `pnpm --filter @workspace/api-server run fetch:index-candles` (reuses the live `kite_session` token; pulls 2y of 15-min SPOT candles for NIFTY/BANKNIFTY/SENSEX into `tools/fno-backtester/data/*.csv`; fail-closed on partial fetch; run outside market hours). See "Tier C data-fetch integration" below.
- **Typecheck**: `pnpm run typecheck` (canonical full check; runs `typecheck:libs` + every leaf workspace's `tsc --noEmit`).
- **Tests**: `pnpm --filter @workspace/api-server run test` (vitest, includes live-DB heat-SQL regression that auto-skips when `DATABASE_URL` is unset). `pnpm --filter @workspace/scanner run test` (vitest + jsdom).
- **DB push**: `pnpm --filter @workspace/api-server exec drizzle-kit push` (applies the Drizzle schema in `lib/db/src/schema/` to the dev database).
- **Required env**: `DATABASE_URL`, `APP_ACCESS_PASSWORD`, `SESSION_SECRET`, `TRADINGVIEW_WEBHOOK_SECRET` (all already provisioned).

## Stack

- **Frameworks**: Express 5 (backend), React (frontend)
- **Runtime**: Node.js
- **Language**: TypeScript 5.9
- **ORM**: Drizzle ORM
- **Validation**: Zod v4, drizzle-zod
- **Build Tool**: Vite, esbuild
- **Monorepo**: pnpm

## Where things live

### Code

- `artifacts/api-server/`: Express API backend
- `artifacts/scanner/`: React + Vite frontend (NSE)
- `artifacts/global/`: Global multi-asset React + Vite frontend
- `lib/db/src/schema/`: Drizzle schema (paperTrading, swingScan, optionChainSnapshot, candleWarehouse, etc.)
- `lib/api-spec/openapi.yaml`: OpenAPI specification (source of truth for codegen)
- `artifacts/scanner/src/theme/`: UI theme configurations

### Data infrastructure (full notes in **`docs/data-infrastructure.md`**)

- **Sector / industry mapping** — `artifacts/api-server/src/lib/sectorMap.ts` + diagnostic `GET /api/stocks-to-watch/diagnostics/sector-coverage`.
- **Option-chain snapshots** — `lib/db/src/schema/optionChainSnapshot.ts` + diagnostics `GET/POST /api/option-snapshots/*`. Write-only, does not feed trading decisions. Read-only analytics surface added 2026-05-15: `GET /api/option-snapshots/analytics` (owner-only, strict gate) computes PCR / total OI / OI deltas / highest-OI strike per side / highest-positive-OI-change / approx max-pain / ATM straddle / ATM IV / spread summary / staleness, defaulting to the latest snapshot per (underlying, top-2 most-recent expiries). Pure module: `artifacts/api-server/src/lib/optionSnapshotAnalytics.ts`. Does not feed any trading decision.
- **Candle warehouse** — `lib/db/src/schema/candleWarehouse.ts` + diagnostics `GET/POST /api/candles/*`. Write-only data substrate.
- **Equity sizing helper** — `artifacts/api-server/src/lib/equitySizingHelper.ts` + diagnostics `GET /api/paper/eq/sizing-preview` and `GET /api/paper/eq/candidates-diagnostic`. Read-only preview, mirrors `openPaperEquityTrade`'s 11-gate sequence; never called from any execution path.

### Architecture docs

- **`docs/paper-trader-architecture.md`** — long-form rationale for F&O Phases 1-4, paper-trader Pass-1/2/3 gates, trade-drought fix, observability addendum, OI Lab backfill, Equity Entry-Safety Gate, Account surface, Kite Offline UX, Pro Swing Scanner v3 port. **Read this for the "why".**
- **`docs/combo-paper-trader-design.md`** — Tier-C design note for the `paper_trade_combo` lane. Phase 1 SHIPPED 2026-05-13 (see Combo paper-trader lane below).
- **`docs/data-infrastructure.md`** — per-feature notes for sector map, option-chain snapshots, candle warehouse, equity sizing helper, combo lane.
- **`docs/audit-implementation-status-2026-05-15.md`** — implementation status report for audit Priorities 1-5 (all COMPLETED 2026-05-15) plus pending operator security actions and remaining audit backlog.

### Owner-only Data Infrastructure Health dashboard (Priority 10, 2026-05-15)
Read-only owner-only roll-up at `/infra-health` (nav: INFRA next to ADMIN). Five sections: Security Status, Sector/Industry Mapping, Candle Warehouse, F&O Option-Chain Snapshots (diagnostics + Priority 9 analytics), Equity Risk Diagnostics (incl. on-demand sizing-preview form). Pure severity helpers in `artifacts/scanner/src/lib/infraHealth.ts` (`deriveAgeSeverity`, `deriveCoverageSeverity`, `deriveSnapshotSeverity`, `deriveCandleSeverity`, `deriveSnapshotSectionSeverity`, `formatAge`, `rollUp`). Snapshot section badge AND header roll-up both consume `deriveSnapshotSectionSeverity` so analytics-down with diag-green correctly surfaces as WARN, never OK. Independent 30s wall-clock state so staleness ages in real time even with auto-refresh OFF. Consumes existing endpoints only (`/api/security/audit`, `/api/stocks-to-watch/diagnostics/sector-coverage`, `/api/option-snapshots/diagnostics`, `/api/option-snapshots/analytics`, `/api/candles/diagnostics`, `/api/paper/eq/candidates-diagnostic`, `/api/paper/eq/sizing-preview`). Zero backend, signal, scheduler, ingestion, or schema changes. 16 unit tests in `infraHealth.test.ts`.

## Architecture decisions

### Cross-cutting
- **Dev-vs-prod paper-trading isolation (2026-05-13)**: Dev/Workspace preview is **read-only** for the paper auto-trader by default — `runEquityPaperTradingTick`, `tryOpenPaperTrades`, `reconcileMissingPaperTrades`, and the inner `openPaperTrade` (FO) all early-return when `isPaperAutoTradingEnabled()` is false. Resolution: `PAPER_TRADING_ENABLED` env override (`1`/`true`/`yes`/`on` enable, anything else disable, fail-closed on unrecognised) → falls back to auto-detect via `REPLIT_DEPLOYMENT === "1"`. Production deployment has `PAPER_TRADING_ENABLED=true` set explicitly. Manual buys (`POST /paper/positions/eq/manual`) and manual closes are NOT gated. UI: `EnvironmentBanner` on `/paper-trading` (green when prod+live, amber otherwise). Diagnostics: `GET /paper/diagnostics/environment` (public, no secrets — only `env`/`autoTradingEnabled`/`reason`). EQ mark-to-market still runs in dev so existing OPEN positions move correctly.
- **Secure Authentication**: HMAC-SHA256 HttpOnly session cookies with role-based access control.
- **Public Access Mode**: Read-only access to the entire site via a shareable URL for unauthenticated users.
- **Legal Pages Accessibility**: `/legal/*` paths bypass login with a stripped-down UI.
- **Data Sourcing Priority**: Kite Connect for real-time data, Yahoo Finance fallback for delayed.
- **Compliance-driven Signal Labels**: API/DB use `STRONG_BUY/BUY/NEUTRAL/SELL/STRONG_SELL`; UI renders `STRONG BULLISH/BULLISH/NEUTRAL/BEARISH/STRONG BEARISH` for compliance.
- **Kite Offline UX**: When Kite session expires the server falls back to Yahoo and panels go sparse; `KiteOfflineBanner` (Scanner / Stock Detail / Deep Scan) and `KiteOfflineNote` (fundamentals, Deep Scan snapshot) tell the owner before they think the app is broken. Owner-only "Reconnect Zerodha" CTA. See architecture doc for branching copy + dev `?mockProvider=` override.

### F&O signal pipeline (full detail in `docs/paper-trader-architecture.md`)
- **F&O universe = NIFTY / BANKNIFTY / SENSEX only** (`OPTION_INDICES` in `optionSignals.ts`, `FNO_INDICES` in `oiLab.ts`, `SIGNAL_INDEX_TO_LTP_KEY`). To restore an index: re-add to all three.
- Phase-1 regime + IVR/IVP + sticky daily/weekly DD caps; Phase-2 EMA20/50 + intraday VP; Phase-3 confluence engine REPLACES per-detector confidence (legacy preserved in `lib/optionSignals.legacyEmit.bak.ts`); Phase-4 KiteTicker WebSocket for index spots.

### Paper-trader safety nets (active guardrails — constants in `paperAccount.ts`)

| Gate | Constant / Trigger | Audit tag | Effect |
|---|---|---|---|
| F&O option-leg liquidity | `FNO_LIQUIDITY` (LTP≥20, spread≤1.5%, OI≥50k) | — | Reject open; fail-OPEN |
| F&O 15:20 IST force-exit | `forceCloseAllOpenFnoFor1520`, latch `lastForceExit1520Date` | `TIME_EXIT_1520` | Close all open FNO |
| Equity stop-loss sanity | `EQUITY_STOP_SANITY` (1%–8%) | — | Reject swing entry |
| Equity DD caps | `EQUITY_DD_CAPS` (2/4/8% of ₹10L) | — | Block opens; sticky latches |
| F&O DD caps | `MAX_DAILY_LOSS_PCT=0.025` / `MAX_WEEKLY_LOSS_PCT=0.05` | `DAILY_DD_CAP` / `WEEKLY_DD_CAP` | Block opens; sticky latches |
| Vol-clamped stop | `VOL_CLAMP_REJECT_RATIO=1.5` | `VOL_CLAMPED_STOP` | Reject above ratio; below → demote HC→BASELINE |
| HTF (daily-EMA50) | `ctx.htfBias` opposes direction | — | Demote HC→BASELINE |
| True 1h HTF | EMA9/21 stack on session-aware 60m bars | `HTF1H_CONFLICT` | Demote HC→BASELINE |
| Time-of-day | 09:15–09:30 / 15:15–15:30 IST | `OPENING_NOISE` / `CLOSING_NOISE` | Demote HC→BASELINE |
| Expiry-day | `regime==="EXPIRY_DAY"` | `EXPIRY_DAY` | Demote HC→BASELINE |
| Sector relative strength | `RELATIVE_STRENGTH.TOLERANCE_PCT=1.0` (NIFTY exempt) | `RS_CONFLICT` | Demote HC→BASELINE |
| 30-day setup win-rate | `WIN_RATE_CALIBRATION {LOOKBACK:30, MIN_SAMPLE:10, MIN_WR:0.4}` | `LOW_WINRATE` | Demote HC→BASELINE |
| ATM-strike OI confluence | Both legs vote against direction (`|atmVote|≥2`) | `OI_ATM_CONFLICT` | Mutate `tier="BASELINE"` |
| Post-stop cool-down | 60min, 0.5× lot multiplier; index-scoped | — | Sizing scale |
| VOLATILE regime sizing | `REGIME_SIZING.VOLATILE_MULT=0.5` | — | Sizing scale |
| Portfolio heat cap | `PORTFOLIO_HEAT.MAX_FNO_HEAT_PCT=0.06` / `MAX_EQ_HEAT_PCT=0.06` | — | Reject open; FAIL-CLOSED, runs in same `FOR UPDATE` tx |

**Combined `isDemoted` partition**: `volClamped || htfConflictGate || noiseWindow || inExpiryDay || htf1hConflictGate || rsConflictGate || lowWinRateGate`. Demoted setups partition OUT of top-3 HC pool BEFORE slicing, append as BASELINE-tier extras. ATM-OI gate is OUT of the partition (post-emission tier mutation). All P3 gates **fail-OPEN** on data failure.

**Sub-tiered BASELINE sizing (2026-05-11)**: `FNO_BASELINE_RISK` (MICRO 0.25 % conf 55-59 / BASELINE 0.5 % conf 60-64 / STANDARD 2 % conf 65+) + `FNO_BASELINE_GUARDRAILS` (max 2 BASELINE/day, 0.75 % daily loss cap incl. unrealised, 2-loss lane lock, 14:45 IST late-entry cutoff). `getBaselineDayStats` fails CLOSED via `BASELINE_GUARDRAIL_STATS_UNAVAILABLE`.

**Active dial overrides**: `PAPER_FIXED_LOTS = {NIFTY:10, SENSEX:40, BANKNIFTY:30}` (overrides dynamic budget for STANDARD-tier opens); `CONFIDENCE_THRESHOLDS.MIN_FNO_TRADE = 65` (aligned with `HC_EMISSION_FLOOR`).

### Equity Entry-Safety Gate (equity swing only)
`computeEntrySafety()` in `lib/scoring.ts` surfaces `entryQuality` GOOD/FAIR/POOR + `entryPlan`. POOR demotes `STRONG_BUY→BUY` (audit `LATE_ENTRY_AT_RESISTANCE/SUPPORT`), naturally blocking paper-trader auto-opens which require `STRONG_BUY`. UI: `EntryPlanCard` on stock-detail.

### Technical Analysis (NIFTY 500) — Pro Swing Scanner v3
TS port of the Python "Pro Swing Scanner v3" surfaced as a third section on `/stocks-to-watch`. Pure-math `lib/swingScanner.ts` + `lib/swingScannerData.ts` (Kite-first, Yahoo fallback) + `lib/swingScannerStore.ts` (deep scan once-per-day after 15:35 IST + 15min intraday LTP refresh). API: `GET /api/stocks-to-watch/analysis`. Cold-start latch keys off `swing_scan_run` audit row, not result rows. Single-replica assumption.

### OI Lab
- **Δ-window Kite-historical backfill**: First `fetchOiInsights` per (underlying|expiry|day) when buffer < 8 snaps fires background `kc.getHistoricalData(..., oi=true)` for ATM±7 strikes × 2 sides. Same-ts merge never overwrites live snaps. WORKERS=2 per index. Fail-OPEN.

### Options Strategy surface
- `/strategies` has two tabs: **Recommended Plans** (existing 13-strategy regime-ranked bundle, `GET /options/strategies/:underlying`) and **Custom Builder** (`StrategyBuilder` in `pages/strategies-builder.tsx`, max 8 legs, scenario sliders for spot ±20% / IV ±50% / days passed). Builder calls `POST /options/strategies/:underlying/custom` (debounced 300ms) which delegates to `buildCustomStrategy` + `simulateScenario` in `lib/optionStrategies.ts` — both reuse `buildPayoff`, `distributionalMetrics`, `computeLegEdges`, `estimateMargin`, `classifyLegQuality`, `netGreeks`, `netDebit`, `midOrLtp`, `legLiquidity`. **Zero math duplication client- or server-side.** Builder is read-only — no order placement, no paper-trade integration in v1.
- Option chain has an `oiSpike` filter (Unusual OI Buildup): `OI≥5000 AND |ΔOI/OI|≥15%`. Pure helper in `artifacts/scanner/src/lib/optionChainFilters.ts` (`applyStrikeFilter` + constants). Existing volume filter renamed to "Unusual Vol" for clarity.

### Combo paper-trader lane (Tier C, Phase 1, 2026-05-13)
Owner-only manual multi-leg paper trades, fully isolated from the auto `paper_trade_fo` lane.
- **Tables** (`paper_trade_combo` + `paper_trade_combo_leg`): UNIQUE(combo_id, leg_index), 6 CHECK constraints, FK CASCADE. Combo legs persist `qty = lots × lotSize` (shares) and `lots` is taken straight from the request, never reverse-derived. P&L formula `Σ sign·(last−entry)·qty` therefore yields rupees.
- **Routes** (`/api/paper/combos`): owner-only. POST opens, GET lists/details (re-marks live on detail), POST `:id/close`. All pricing comes from `fetchOptionChain` server-side; the request schema does not accept premium/IV/Greeks/margin/P&L — `sanitizeLegSpec` strips them defense-in-depth.
- **Defined-risk only v1**: rejects naked shorts/ratios via `snapshot.maxLoss == null` → `UNDEFINED_RISK` 400.
- **Concurrency safety**: open-cap (`COMBO_MAX_OPEN`) gated inside the insert txn under `pg_advisory_xact_lock(7593721)`; close uses CAS `WHERE id=? AND status='OPEN'` and returns "already closed" for the loser of a race.
- **Isolation**: auto-trader paths (`runFnoPaperTradingTick`, `tryOpenPaperTrades`, etc.) only touch `paper_trade_fo`/`paper_trade_eq`; combo lane never enters the FNO heat budget and is **opted out of the 15:20 force-exit**.

### Account surface
- `/paper-trading` is live-only; closed history / equity curve / analytics / journal live in `/paper-reports`.
- Equity Account Card: Capital sub-grid + Open-portfolio MTM sub-grid. `lifetimeRealizedPnl` is server-side (top-up safe).

### Portfolio Analyser (read-only, Phase 1, 2026-06-02)
Owner/subscriber tab at `/portfolio-analyser` (nav: "Portfolio", `tab: "PORTFOLIO_ANALYSER"` registered in `App.tsx`, `layout.tsx`, `auth-api.ts`, `lib/db/src/schema/users.ts`, `admin.tsx` — the schema entry is a string allow-list constant, **not** a DB migration). **Pure frontend, client-session-only** — no backend/DB/trading/scheduler/env changes; holdings live in React state only (never persisted). Real data via existing generated hooks (`getStockDetail` through `useQueries` for the table; `useGetStockDetail` + `useGetNews` in the deep-dive). **No silent mock data** — genuinely-unavailable data is explicitly labelled (live CMP unavailable, "Benchmark unavailable" for absent Nifty-500 sector weights, XIRR-unavailable). **SEBI-neutral**: only the neutral action-view vocabulary from `score.ts` (`Strong Structure / Hold with Review / Mixed / Watch / Weak Structure / Reduce Review / Exit Review / Avoid Fresh Buy`); support/resistance rendered only as "zones"; the tab NEVER surfaces `recommendation.target/stopLoss/displayLabel/signal`. Pure tested libs in `artifacts/scanner/src/lib/portfolio/` — `types.ts`, `csv.ts` (template + quoted parser + row validation), `calc.ts` (per-holding metrics, summary, sector allocation, Newton-Raphson XIRR), `score.ts` (transparent composite from AVAILABLE signals only + neutral label + risk flags). Sample portfolio is opt-in with a persistent amber "preview only" banner (real tickers/quotes; only qty/rate/date are illustrative); "Load from Database" is disabled (not configured in v1). Tests: `csv.test.ts`, `calc.test.ts`, `score.test.ts` (395/395 scanner tests pass).

### Portfolio Analyser Phase 2 — Save & Enrich (2026-06-03)
Makes Phase-1 portfolios **DB-persisted per-user** (survive refresh) and adds honest enrichment panels. **Per-user privacy**: new tables `portfolios` + `portfolio_holdings` (`lib/db/src/schema/portfolio.ts`, `ownerKey`-scoped FK); CRUD routes in `artifacts/api-server/src/routes/portfolio.ts` mounted under `/portfolios/*` (owner OR subscriber, each sees only their own rows — **no public/shared access**; unauth → empty/403). OpenAPI paths+schemas added to `lib/api-spec/openapi.yaml` → generated `Portfolio/PortfolioHolding/PortfolioHoldingInput/PortfolioSummary` DTOs + async client fns. Express-5 gotcha: `req.params["id"]` is `string|string[]` — use the `paramId(req)` helper. **Advisory fields (targetPrice/stopLoss) are intentionally NOT persisted** — only user book-keeping (qty/rate/date/broker/tag/notes/isin/dividendReceived/realisedPnl).
- **Frontend persistence**: `artifacts/scanner/src/lib/portfolio/persistence.ts` — pure mappers `holdingToRaw`/`rawToInput` + `usePortfolios()` React-Query hook. Page (`pages/portfolio-analyser.tsx`) auto-loads the default portfolio on mount, dirty-tracks via a holdings signature, and exposes switch/save/save-as/rename/set-default/delete via `components/portfolio/portfolio-toolbar.tsx`. Sample data stays save-disabled.
- **New pure tested libs** (`artifacts/scanner/src/lib/portfolio/`): `returnLabel.ts` (XIRR vs Annualised-Estimate vs Unavailable labelling — fixes the misleading "approx XIRR" when dates are missing), `risk.ts` (HHI/top-weight/**top-3 combined weight**/**top-sector weight over sector-covered value + honest `sectorCoveragePct`**/value-weighted-beta+coverage/contributors/data-availability + flags `SINGLE_STOCK_CONCENTRATION`/`TOP3_CONCENTRATION` (>60%, only when >3 holdings)/`SECTOR_CONCENTRATION` (>35% of classified value)/`HHI_HIGH`/`LOW_DATA_AVAILABILITY`, `RISK_THRESHOLDS`), `allocation.ts` (sector/stock/market-cap/P&L/win-loss views, `MARKET_CAP_BUCKETS`; market-cap honestly unavailable when no mcap), `holdingPeriod.ts` (`LONG_TERM_THRESHOLD_DAYS=365` config + dividend totals/yield-on-cost; `computeDividends` total-return-incl-income = currentValue + dividends + realised P&L − invested, i.e. realised P&L IS folded in), `benchmark.ts` (`benchmarkReturnFromCloses` computes a real buy-and-hold return from a supplied index close-series; sector over/under-weight still explicitly **not fabricated**).
- **UI panels** (`components/portfolio/analytics-panels.tsx` + `methodology.tsx`): Risk, Allocation (mode toggle), Cost-Basis & Income, Benchmark, plus a collapsible Methodology disclosure. KPI strip now uses `returnLabel`; Day-P&L tile carries a formula tooltip (`qty×(CMP−prevClose)`). Deep-dive: bare em-dash stats render as labelled "n/a" with a "not reported by source" tooltip; structure-score shows a 0–100 scale tooltip (bands 0–39 weak / 40–59 mixed / 60–79 constructive / 80–100 strong); news section labelled "<SYMBOL> only". **No ADX anywhere** — score is the transparent `score.ts` composite (confirmed).
- **Benchmark wiring (2026-06-03)**: page fetches the real NIFTY 50 daily series via the existing `getChartCandles({symbol:"NIFTY",segment:"index",tf:"1D"})` (Kite→Yahoo), windows it from the earliest holding purchase date, and computes the relative return via `benchmarkReturnFromCloses`. The window is labelled with the ACTUAL first covered candle date (so it is never overstated when available history is shorter than the holding period); if the fetch yields no closes it falls back to the explicit "unavailable" state. Sector over/under-weight remains honestly unavailable (no Nifty-500 weight reference).
- **Benchmark performance chart (2026-06-03)**: the Benchmark panel now renders a small Recharts `LineChart` of the selected index rebased to % change from the window start (pure tested helper `buildBenchmarkSeries(candles)` in `benchmark.ts` — empty array when <2 finite closes or zero base, never fabricated; its final point's `indexPct` equals `benchmarkReturnFromCloses` over the same closes by construction). The page shares one `benchmarkWindowed` memo between the headline return and the chart so they always agree. The portfolio's own path is NOT reconstructable from a single live CMP per holding, so the portfolio's known total return over the window is drawn as a labelled dashed horizontal `ReferenceLine` (honest endpoint, not a fabricated path). Honest states: loading skeleton, and an explicit "no index series available" empty box (never a blank/fake chart). `BenchmarkChart` lives in `analytics-panels.tsx`; tests in `benchmark.test.ts` (516/516 scanner, typecheck green).
- **DMA candle fallback (2026-06-03)**: stock-deepdive sources 50/200-DMA as enriched/keyStats first, then a candle-derived `sma()` from real daily closes; missing → "Insufficient history" (with bar count) or "n/a", never a fabricated number.
- **Round-4 hardening (2026-06-03)**: (1) Risk panel now surfaces **top-3 combined weight** + **top-sector weight** rows (amber when over threshold) on top of single-stock/HHI. (2) **CSV export** — `buildPortfolioCsv(holdings)` in `csv.ts` round-trips through `parsePortfolioCsv` (template columns only; advisory targetPrice/stopLoss + derived dividend/realisedPnl intentionally excluded); wired via toolbar "Export CSV" → `exportCsv()` Blob download in the page. (3) **No bare em-dashes** in `holdings-table.tsx` — unavailable CMP/derived/score cells render a labelled `n/a` with a reason `title` (local `Unavailable` helper); `format.ts` `fmt*` left untouched globally. (4) `BenchmarkPanel` adds an explicit note that **NIFTY 500 and sector-index comparisons are unavailable** (not wired; would need a new data source) — never fabricated.
- **Strict scope**: NO changes to trading/F&O/paper-exec/stop-target-sizing/scanner-scoring/Kite-auth/scheduler. Tests: `returnLabel/risk/allocation/holdingPeriod/benchmark/persistence/csv.test.ts` (475/475 scanner, 812/812 api-server, full typecheck green). **Not published** — owner to deploy when ready.
- **NIFTY 500 benchmark + sector over/under-weight (2026-06-03)**: NIFTY 500 added as a selectable benchmark (`BENCHMARK_OPTIONS` key/symbol `NIFTY500`, name "NIFTY 500"); resolved via the existing chart endpoint because `NIFTY500` is now in `CURATED_INDICES` (`chartInstruments.ts`, yahoo `^CRSLDX`, Kite has no token → Yahoo fallback). Sector over/under-weight is now COMPUTED (no longer "always unavailable") from a real, dated reference: `NIFTY500_SECTOR_REFERENCE` in `benchmark.ts` is the published NSE NIFTY 500 industry-weightage table captured `NIFTY500_SECTOR_REFERENCE_AS_OF=2026-06-03`, rolled up to this app's sector vocabulary as a true partition (each NSE industry row assigned to exactly one bucket; sums to ~100%, never double-counts). `compareSectorWeights()` aggregates portfolio weight into reference buckets via `normalizeSectorKey()` (exact + lower-cased synonym alias map; Pharma→Healthcare, Oil&Gas/Power→Energy, etc.), reports per-bucket portfolio/benchmark/diff + stance (over/under/in-line, ±0.5pp tolerance), surfaces unmapped sectors (e.g. Cement, Consumer Internet) honestly under "not benchmarked", and reports `coveragePct`. **Honest by construction**: unmapped sectors never forced into a weight; whole comparison reports unavailable when no live sector weights exist; reference is a STATIC dated snapshot (labelled in-panel). `BenchmarkPanel` renders the new sector table + coverage note. Broad-market sector index *return* series (selectable per-sector benchmark) NOT added — out of scope. Tests: `benchmark.test.ts` extended (partition-sum, alias mapping, aggregation, unmapped/coverage, sort); 487/487 scanner, full typecheck green.
- **Per-sector index return (2026-06-03)**: each HELD sector now shows its OWN NSE sectoral-index return over the SAME window as the headline benchmark, alongside the existing over/under-weight row (new "Idx ret." column in `BenchmarkPanel`). Eight NSE sectoral indices added to `CURATED_INDICES` (`chartInstruments.ts`, `type:"Sector Index"`, Kite has no historical token → Yahoo fallback, same pattern as NIFTY500): NIFTYIT `^CNXIT`, NIFTYAUTO `^CNXAUTO`, NIFTYPHARMA `^CNXPHARMA`, NIFTYFMCG `^CNXFMCG`, NIFTYMETAL `^CNXMETAL`, NIFTYENERGY `^CNXENERGY`, NIFTYREALTY `^CNXREALTY`, NIFTYMEDIA `^CNXMEDIA`. `benchmark.ts` adds `SECTOR_INDEX_MAP` (bucket → {sector,symbol,name}; Banking→BANKNIFTY, Financials→FINNIFTY, IT→NIFTYIT, Auto→NIFTYAUTO, Healthcare→NIFTYPHARMA, Metals→NIFTYMETAL, Energy→NIFTYENERGY, FMCG→NIFTYFMCG, Real Estate→NIFTYREALTY, Media→NIFTYMEDIA), `sectorIndexFor()` (reuses `normalizeSectorKey` aliases so Pharma→NIFTY PHARMA, Oil&Gas→NIFTY ENERGY), and `sectorIndexesForSectors()` (distinct, de-duplicated refs for held sectors only). Page (`pages/portfolio-analyser.tsx`) fetches one daily series per relevant index via `useQueries` + the existing `getChartCandles`, windows to earliest-purchase cutoff, computes return via `benchmarkReturnFromCloses`, passes a `Map<sector,SectorIndexReturn>` to the panel. **Honest by construction**: sectors with NO published NSE sector index (Insurance, Telecom, Capital Goods, Construction, Consumer Discretionary, Defence, Chemicals, Logistics, Aviation) map to null → render "—"; loading → "…"; series with no closes → "n/a"; return is NEVER fabricated. Pure-frontend, zero backend/DB/schema/trading change apart from the additive CURATED_INDICES rows. Tests: `benchmark.test.ts` extended (`SECTOR_INDEX_MAP` partition/key match, alias resolution, no-index nulls, dedup/zero-weight filtering); 521/521 scanner, full typecheck green.
- **Portfolio value path on benchmark chart (2026-06-04)**: the Benchmark panel's chart now draws the portfolio's reconstructed value path as a SECOND line (rebased % from window start) alongside the index line, replacing the single horizontal total-return reference (the reference line is now only a fallback when no path can be built). Pure tested helpers in `benchmark.ts`: `buildPortfolioValueSeries(holdings)` = Σ qty × daily close, but ONLY over days where EVERY covered holding has a close (constant-basket → true % move, no artificial jumps), rebased from the first fully-covered day; `mergeBenchmarkAndPortfolio(indexSeries, portfolioPoints)` aligns both onto one sorted date axis with honest nulls (chart uses `connectNulls`). **Honest by construction**: holdings with no daily history are surfaced in `missingSymbols` and excluded (never imputed); `partial` flags when the view omits holdings or starts later than earliest data; empty `points` + reason when no overlapping history (panel falls back to the flat total-return reference). Path basis is window-start MARKET value (not cost basis) — labelled explicitly under the chart so it never reads as the cost-basis "Portfolio return" row. Page (`pages/portfolio-analyser.tsx`) fetches one daily series per holding via `useQueries` + existing `getChartCandles({segment:"equity",tf:"1D"})`, windows to earliest-purchase cutoff. Pure-frontend, zero backend/DB/schema/trading change. `BenchmarkChart`/`BenchmarkPanel` take combined `CombinedSeriesPoint[]` + `PortfolioValueSeries`. Tests: `benchmark.test.ts` extended (constant-basket, missing-symbol exclusion, no-overlap/no-history unavailable, non-finite guards, merge alignment); 34 benchmark tests pass, full typecheck green.
- **ETF live-quote path (2026-06-03)**: ETFs (NIFTYBEES/GOLDBEES/BANKBEES) previously showed "CMP unavailable" because `/stocks/:symbol` requires BOTH a curated metadata entry AND a scored `scanAll()` row (404 for ETFs). New lightweight dedicated path: `ETF_WHITELIST` + `isWhitelistedEtf()` + `loadKiteEtfQuote()` (reuses `loadKiteQuotes([sym])`, returns null when Kite offline) in `artifacts/api-server/src/lib/kiteScanner.ts`; owner/subscriber route `GET /etf/:symbol/quote` in `routes/scanner.ts` (404 if not whitelisted, 503 `KITE_OFFLINE` when Kite down — **never a faked price**). OpenAPI `EtfQuote` schema + `getEtfQuote` op → generated client fn + `GetEtfQuoteResponse` zod. Frontend: `enrich.ts` Step 1b cascade branch (gated on `isEtfClass(classifyInstrument(...))`, runs after stockDetail / before search) maps the quote via `liveFromEtfQuote()` with `dataSource:"etf-quote"`, `fundamentalsApplicable=false` (ETF fundamentals degrade honestly as "not applicable" — no PE/etc.); `etfQuote` fetcher wired into FETCHERS in `pages/portfolio-analyser.tsx`. Tests: 3 new `enrich.test.ts` cases (478/478 scanner, 830/830 api-server, full typecheck green).
- **Cross-device benchmark sync (2026-06-04)**: each saved portfolio's chosen benchmark now persists server-side (new nullable `benchmark` text column on `portfolios` in `lib/db/src/schema/portfolio.ts`) so the choice follows the user across devices/browsers instead of being trapped in per-device localStorage. Threaded through OpenAPI (`Portfolio`/`PortfolioCreateBody`/`PortfolioPatchBody` gain a nullable `benchmark`; codegen re-run) and `routes/portfolio.ts` (opaque `benchmarkOrNull()` shape-guard `/^[A-Z0-9]{1,32}$/` → null for blank/invalid so a stale key never sticks; create persists it, PATCH can set/clear it standalone via a `"benchmark" in body` presence check, PUT-holdings leaves it untouched, GET returns it). Frontend (`persistence.ts` `create(..., benchmark?)` + new `setBenchmark(id, key)`; `pages/portfolio-analyser.tsx` `benchmarkForLoaded()` prefers the server value via `isBenchmarkKey` then falls back to `resolveBenchmarkPref(id)`). **localStorage (`benchmarkPref.ts`) remains the graceful fallback** for unsaved/sample working sets and pre-existing saved portfolios with no stored key, and is still written on every select so a per-device cache stays warm. Honest "unavailable" benchmark fallback unchanged. Tests: 7 new live-DB cases in `portfolioRouteIsolation.test.ts` (create/read, default-null, PATCH-only set, null-clear, malformed→null, survives PUT, cross-user 404); 890/890 api-server, 546/546 scanner, full typecheck green.

- **ETF NAV premium/discount (2026-06-04)**: ETF Profile in the Portfolio Analyser deep-dive now shows the real NAV and live premium/discount % vs CMP (previously "NAV not tracked"). **Real source, never fabricated**: backend `artifacts/api-server/src/lib/etfNav.ts` fetches the official **AMFI** `NAVAll.txt` feed (semicolon-delimited, keyed by ISIN; 1h in-memory TTL, 12s timeout, returns null on failure), parsed via `parseAmfiNav` (drops non-numeric/≤0 NAVs, indexes both growth+reinvestment ISINs). Symbol→ISIN via curated `ETF_ISIN_MAP` (21 whitelisted ETFs, ISINs verified against the live feed `ETF_ISIN_MAP_AS_OF=2026-06-03`) with optional `?isin=` override (the user's own holding ISIN, so uncurated ETFs still work). Route `GET /etf/:symbol/nav` in `routes/scanner.ts`: 200 `{symbol,isin,nav,navDate,schemeName,source:"AMFI"}`, 404 no-mapping/not-in-feed, 503 `NAV_FEED_OFFLINE`. OpenAPI `EtfNav` schema + `getEtfNav` op → generated client + `GetEtfNavResponse` zod. **AMFI NAV is END-OF-DAY (T-1), not a live intraday iNAV** — so premium/discount is approximate/lagged and the UI labels the `navDate` + carries an explicit note. Frontend: pure `computeNavPremiumDiscount(cmp,nav)` in `lib/portfolio/etf.ts` (premiumPct=(CMP−NAV)/NAV×100, stance premium/discount/fair within `NAV_FAIR_TOLERANCE_PCT=0.1`, null on bad input); `stock-deepdive.tsx` adds NAV + `PremiumDiscountStat` tiles (premium red / discount green / fair muted), honest "unavailable"/"Loading…" states. Equity holdings unaffected (only renders when `!fundamentalsApplicable`). Tests: `etfNav.test.ts` (api-server) + `etf.test.ts` extended (876 api-server, 528 scanner, full typecheck green).

### Navigation restructure — grouped nav (2026-06-02)
`artifacts/scanner/src/components/layout.tsx` top-level nav was reorganised from ~21 flat tabs into professional grouped dropdowns (Radix `DropdownMenu`). **Presentation-only — zero route/page/data/feature/auth/API/DB/schema/env/scheduler change.** Groups: **Home** (link), **Stock Intelligence** (Full Scanner / Deep Scan / Watchlist / Sector Rotation / To Watch), **Derivatives** (Option Chain / OI Lab), **Trading Desk** (F&O Intraday / Strategies / Paper Trading* / P&L Reports*), **Market Pulse** (Pre/Post / Market Info / FII·DII / Live Feed*), **Charting** (link), **Portfolio** (link → Portfolio Analyser), **Learn** (link), **Admin*** (Admin Console / Audit / Status / Infra). `*`=owner-only. Each leaf keeps its **original href** and its `tab`/`ownerOnly` visibility tag; a new `desc` page-purpose label renders in each dropdown (direct links carry it as a `title` tooltip). Visibility helper `canSee()` is functionally identical to the old flat filter (owner→all; `ownerOnly`→owner-only; tab-gated→`allowedTabs.includes(tab)`); a group renders only if ≥1 child is visible, so **no owner-only page is ever exposed to subscribers/public**. Top-right ADMIN + INFRA quick-access chips left untouched (dual access; report notes the redundancy). Dropdown content portals (Radix `Portal`) so it is not clipped by the nav's `overflow-x-auto`; `data-active` preserved on links + group triggers for the existing active-tab auto-scroll. Deferred to a future phase **as recommendations only** (not implemented, to stay within the safe nav-only scope): cross-page empty-state copy, data-source-badge wording consistency, and compact/collapsible ticker. Full mapping + report: `docs/navigation-restructure-2026-06-02.md`. Architect review PASS (no visibility/security regression); 395/395 scanner tests + typecheck green.

### P22: All-Open F&O Paper-Trade MTM Coverage Fix (2026-05-19)
Closes the MTM-staleness gap noted at P21b EOD: `markOpenFnoTradesToMarket` only updates OPEN rows whose (index, setup, direction) is in the **current** signal cohort, so rows that drop out went idle on `last_premium`/`max_runup`/`max_drawdown` until 15:20 force-exit. Now a second chain-driven sweep `markAllOpenFnoTradesToMarket(signalDate)` runs immediately after the cohort path in `getOptionSignals` and refreshes every OPEN row by looking up its stored `strike`/`optionType` in a freshly fetched chain for its `indexSymbol`. Pure helper `pickLtpFromChain(chain, strike, optionType)` does the lookup (epsilon-tolerant strike match for numeric(_,4) round-trip jitter). Updates only `last_premium`, `last_evaluated_at`, `max_runup` (GREATEST — monotone), `max_drawdown` (LEAST — monotone). 45-second freshness window (`MTM_FRESHNESS_WINDOW_MS`) skips rows already refreshed by the cohort path. Per-index batching (≤3 `fetchOptionChain` calls per cycle for NIFTY/BANKNIFTY/SENSEX; existing ~5s TTL cache makes repeats free). Top-level + per-row try/catch; caller wraps in `.catch(...)` — sweep failure can never break the signal loop. Optional `dbHandle` param is a test-injection seam (`Pick<typeof db, 'select' | 'update'>`); production passes nothing. Diagnostics: `getMtmSweepHealth()` returns `{cyclesTotal, rowsUpdatedTotal, lastCycle:{considered, updatedFromChain, skippedAlreadyFresh, skippedNoQuote, errors}, lastSuccessAt, lastErrorAt/Class/Message}`. 16 tests in `paperTradingFoMtmSweep.test.ts` (DB-rollback pattern; everything inside one `tx`, `tx.rollback()` at end → zero footprint on dev DB; auto-skip when `DATABASE_URL` unset). 493/493 api-server tests pass.

**Approved behaviour impact (sign-off 2026-05-19)**: `last_premium` is read by two paths beyond pure display — `pickExitPremium()` (TIME_EXIT_1520 / EXPIRED / MANUAL_OVERRIDE close settlement → realized P&L) and `getBaselineDayStats()` (`entry_premium − last_premium` → BASELINE 0.75 % intra-day loss-cap). Fresher MTM therefore makes BOTH **more accurate**, not less. Explicitly approved as a welcome side-effect — *not* a violation of P22 strict scope. **Unchanged**: signal generation, entry conditions, stop-loss / target formulas, sizing, gate thresholds, Kite execution, swing scanner, equity paper-trader, scanner, strategy builder, combo lane, snapshot/candle ingestion, scheduler, schema.

**Rollback**: remove the `await markAllOpenFnoTradesToMarket(...)` call site in `optionSignals.ts` (one block). The function and its tests can stay dormant; no schema migration, no scheduler change, no data backfill is required either way.

### Shadow F&O Cost / Slippage / Spread Model (P17b, 2026-05-17)
Reporting-only cost overlay. Pure deterministic model `artifacts/api-server/src/lib/fnoCostModel.ts` (`computeFnoTradeCost` + `FNO_COST_PARAMS`: brokerage ₹20/side, STT 0.0625% sell premium, exchange 0.03503%, SEBI 0.0001%, GST 18%, stamp 0.003% buy, spread 25bps/side, slippage 10bps/side). Aggregator `fnoShadowCosts.ts` joins `paper_trade_fo` (CLOSED) ↔ `option_signal_history` for tier, groups gross-vs-shadow-net by setup/index/tier/exitReason, surfaces top-N "flipped-to-loss" trades. Owner-only endpoint `GET /api/paper/analytics/fo/shadow-costs?from=&to=&topNFlipped=`. UI section on `/infra-health` ("F&O Shadow Costs"). Feature flag `PAPER_FO_COSTS_SHADOW_ENABLED` (default ON; only gates reporting). **NO** changes to signal/entry/exit/stops/targets/sizing/gates/confluence/paper-exec/realised P&L/DD caps/circuit-breakers/heat/Kite/swing/equity/scanner/strategy/combo/snapshot/candle/scheduler/schema. Distinguishes exit=null (open: 1-side brokerage, gross/net=null) from exit=0 (expired worthless: 2-side brokerage, STT=0, gross computed). 22 unit tests in `fnoCostModel.test.ts` (446/446 api-server tests pass).

### F&O Observability Substrate (P17a, 2026-05-17)
Diagnostics-only repair so future sessions populate `fno_signal_reasoning`, `paper_daily_summary_fo.skipped_by_reason`, exact `signal_fingerprint`, and `setup_key`. **NO** changes to signal generation, entry, exit, sizing, stops, targets, gates, confluence, paper-trade exec, Kite, swing, paper-equity, scanner, strategy, combo, option snapshot, candle ingestion, or scheduler. Three pieces:
1. **Process-local logger health counters** (`fnoSignalReasoningLogger.ts`): `getReasoningLoggerHealth()` returns `{writesAttempted, writesSucceeded, writesFailed, lastSuccessAt, lastErrorAt, lastErrorClass, lastErrorMessage (≤200 chars), bootedAt}`. `__resetReasoningLoggerHealthForTests()` for unit tests. Fire-and-forget contract preserved.
2. **Durable skip-reason fallback** (`paperDailySummaryFo.ts`): `fetchDurableSkipReasons(date)` consults `fno_signal_reasoning WHERE decision IN ('SKIPPED','MISSED_WINDOW')` when the in-memory `getMissedSignals()` ring is empty for the IST date (ring resets on every process restart). Fail-OPEN: returns empty histogram on DB error.
3. **Owner-only diagnostics endpoint** `GET /api/paper/diagnostics/fno-observability` + UI section ("F&O Observability Substrate (P17a)") on `/infra-health`. Returns verdict OK/WARN/FAIL with reasons, loggerHealth, durable totals (rowsToday, decisionsToday histogram, upstream/downstream split, fingerprint coverage %, skip-durable count), missedRing snapshot, and `paper_trade_fo.setup_key` distribution with validity flags. Known-valid set: `TREND_CONTINUATION/VWAP_RECLAIM/VOLUME_BREAKOUT/EMA_PULLBACK/MEAN_REVERSION/BASELINE` ("BASELINE" is the legitimate always-on directional detector at `optionSignals.ts:985`, not a tier conflation). 5 unit tests in `fnoObservability.test.ts`.

Root cause of empty substrate: logger + table both deployed 2026-05-17 07:41 UTC (Sunday, market closed). 0 rows because `getOptionSignals()` only fires during market hours; auto-trader is OFF in dev (`PAPER_TRADING_ENABLED` unset).

### F&O Failure Diagnosis Report (P16, 2026-05-17)
Owner-only READ-ONLY endpoint `GET /api/paper/analytics/fo/failure-diagnosis` + UI section on `/infra-health` ("F&O Failure Diagnosis"). Pure module `artifacts/api-server/src/lib/fnoFailureDiagnosis.ts` consumes `fno_signal_reasoning` rows (linked via P15b `signal_fingerprint`) and produces 8 sections: A setup table, B index table, C tier table + HC-vs-BASELINE verdict, D lifecycle funnel (EMITTED→OPENED→T1→T2 / →STOPPED conversions via exact fingerprint), E stop-loss deep dive (by setup/index/confidence/regime + after-T1 reversal count), F untriggered/expired (skip-reason histogram + late-session ≥14:00 IST share), G missing-data/demotion (per-field stop-rate correlation), H ten-hypothesis ranking (H1–H10) with each tagged status ∈ {proven, likely, insufficient_data, undetermined} and sample size. Filters: date range, index, setup, tier, direction, decision, reason code, regime, plus new `exactOnly=1` (rows with `signal_fingerprint IS NOT NULL`). Recommendations list only proven/likely hypotheses, ordered by status priority. **NO changes** to signal generation, entry, exit, sizing, stops, targets, gates, confluence, paper-trade exec, Kite, swing, paper-equity, scanner, strategy, combo, option snapshot, candle ingestion, or scheduler. 23 unit tests in `fnoFailureDiagnosis.test.ts` (414/414 api-server tests pass).

### F&O Diagnostics UI + Data-Health Truth Labels (2026-06-05)
Owner-only READ-ONLY visibility layer over the already-merged `/api/fno/*` namespace. **NO** change to entry/stop/target/exit/time-cutoff/detector/confidence/risk-gates/DD-caps/heat-caps/premium-target/realized-P&L/paper-exec/DB-schema/scheduler/env. Three pieces:
1. **Pure backend verdict helpers** (`artifacts/api-server/src/lib/fnoDiagnosticsFacade.ts` + tests): `deriveSpotProvider`, `mapOptionChainProvider`, `deriveSignalReadiness` (returns `signalAllowed`/`blockingReasons`/`blockingSeverity`/`dataSourceVerdict`/`spotProvider`/`optionChainProvider`/`freshEnoughForSignal`/`missingFields`), `computeAtmStraddle` (null + reason `UNAVAILABLE` when ATM legs missing; explicit `formulaLabel`). **Honest by construction**: no fake zeros; non-Kite option data → `NON_KITE_OPTION_DATA` FAIL (never treated as F&O-live); `signalAllowed` true ONLY on fresh live Kite spot + chain + session. Thresholds passed in from the route (route owns `FNO_LIQUIDITY` import → facade stays pure, no constant drift).
2. **Additive route fields** (`routes/fno.ts` `/fno/data-health`): per-index now also attaches the verdict fields + `expectedMove` (ATM straddle). Additive-only; no trading-path mutation. All `/api/fno/*` routes remain `requireOwner` (401 unauth).
3. **Owner-only `/fno-diagnostics` cockpit** (`pages/fno-diagnostics.tsx`, nav "F&O Diag" in Admin group; route guarded `ownerOnly` in `App.tsx`). Sections A–J: Data Health, Signal Allowed/Blocked by index, Kite/feed/chain status, Today (IST), Gate Waterfall, No-Trade Reasons, Setup Performance, Dormant Detectors, Gross-vs-Net cost note, Premium-target informational note. Consumes only existing GET diagnostics endpoints via plain-fetch react-query hooks (`lib/fno/diagnostics-fetch.ts`); pure honest formatters (`lib/fno/diagnostics-format.ts` + tests) render labelled `n/a` with reasons — never fabricated zeros/percentages/verdicts. Truth labels also added to paper-trading UI (`FoCockpitSummaryCards.tsx`, `paper-reports.tsx`, `ReportsOverviewCards.tsx`): headline F&O P&L marked "(gross, pre-cost)", shadow cost/net relabelled "Estimated Costs" / "Estimated Net P&L (shadow-only — not used for DD/heat/risk gates)" — wording only, no calc change. Endpoints NOT in OpenAPI → no codegen. Tests: `fnoDiagnosticsFacade.test.ts` (919 api-server, `--pool=threads`), `diagnostics-format.test.ts` (566 scanner); full typecheck green.

### Diagnostics & observability (Paper F&O)
- `GET /paper/diagnostics/untriggered/fo` — groups skips by reason / index / tier with 50-row recent log.
- `GET /paper/diagnostics/daily-summary/fo` — today's IST-day metrics with two date anchors (opened-today vs closed-today).
- `GET /paper/diagnostics/daily-summary/fo/history?from=&to=` — trailing-30-day persisted history from `paper_daily_summary_fo`.
- `getOperationalAlerts()` — process-level counters (e.g. `baselineStatsUnavailableAlertCount`).

### Finalise F&O — Tier A (safe fixes) + Tier C (in-repo backtester) (2026-06-05)
Diagnostics/labels/tooling only — **NO change to signals, gates, sizing, execution, scheduler, P&L, or env**. Tier B (premium-stop→size, circuit-breaker retune, EMA_PULLBACK quarantine, Signal Core v1) is PARKED until the in-repo backtester proves an edge on real Kite CSV data.
- **Tier C — in-repo Python backtester** (`tools/fno-backtester/`): advisor-validated Python placed VERBATIM (not ported to TS, to keep spec parity) — `fno_cost_model.py`, `fno_backtester.py`, `signal_core_spec.md` (reference), `README.md` (how to run on a Kite 15-min CSV). `numpy`+`pandas` installed via package management. `python3 tools/fno-backtester/fno_backtester.py --selftest` runs end-to-end and prints a synthetic result; the cost-model `__main__` worked example runs. Fully isolated from the live system.

### Tier C data-fetch integration — Kite → backtester CSVs (2026-06-05)
Wires real Kite 15-min candles into the Tier-C backtester. **Isolated tooling only — NO change to live signals/gates/sizing/execution/scheduler/P&L/env.**
- **Wired fetcher** (`artifacts/api-server/src/scripts/fetchKiteIndexCandles.ts`, run via `pnpm --filter @workspace/api-server run fetch:index-candles`): reuses the LIVE Kite session through `getRestClient()` (token lives encrypted in the `kite_session` DB table — **not** env vars), resolves NIFTY 50 / NIFTY BANK / SENSEX via the instrument master (`getInstruments(exchange)`, fallback tokens 256265/260105/265), loops 2y of `15minute` in 100-day chunks (under Kite's ~200-day cap, paced 450ms), dedupes by timestamp + sorts, writes `tools/fno-backtester/data/{NIFTY,BANKNIFTY,SENSEX}.csv` (`date,open,high,low,close,volume`). **Fail-closed**: any chunk error skips that index's CSV and exits non-zero (a partial 2y window would bias the backtest); re-run is idempotent; `-- --allow-partial` to accept gaps. Generated CSVs are gitignored. **Timestamps are IST-local naive** (`YYYY-MM-DD HH:MM:SS`, session 09:15..15:30) via `Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata"})` — NOT `.toISOString()`, which would emit UTC and shift the whole session −5:30h, breaking the backtester's hour buckets / session-VWAP reset / time-of-day gates (first NIFTY row must read `09:15:00`, never `03:45:00`).
- **SPOT, not futures — settled correctness call**: the advisor posed spot (vol=0) vs near-month futures (real vol) and leaned futures. The live engine computes index VWAP from Kite SPOT candles where cash-index volume is 0 (`kiteIntraday.ts` "emit zero"), `indicators.ts sessionVwap` falls back to typical price when cumulative vol is 0 (`out[i] = v>0 ? pv/v : typ`), and volume-breakout/profile detectors stay DORMANT for indices. A backtest must use the SAME volume basis as live → futures would test a strategy live doesn't run = invalid. So we fetch SPOT.
- **Backtester VWAP fix to match live**: `fno_backtester.py session_vwap` previously returned NaN on zero volume (would kill every VWAP-gated signal). Now falls back to typical price when cumulative volume is 0 (`vwap.where(vv>0, tp)`), faithfully mirroring `indicators.ts`. Verified: zero-vol series → typical price (no NaN); real-vol series → true cumulative VWAP. This is isolated backtester logic, NOT a live trading-logic change.
- **Reference Python fetcher** `tools/fno-backtester/kite_fetch_indices.py` kept as an alternative (env-var `KITE_API_KEY`/`KITE_ACCESS_TOKEN` token path) — but that is NOT how this app stores the token, so the TS fetcher is the wired path. Validation: api-server typecheck green; backtester `--selftest` green. Architect review (includeGitDiff) PASS. Not published.
- **Tier A1 — upstream reasoning-log dedupe + archive-first migration** (the #1 priority; fixes ~88× duplicate inflation): additive archive table `fno_signal_reasoning_archive_pre_dedupe` (mirror of current schema) in `lib/db/src/schema/fnoSignalReasoning.ts` + barrel export (`drizzle-kit push` applied). One-time migration script `scripts/src/dedupeFnoSignalReasoning.ts` (`@workspace/scripts`, depends on `@workspace/db`+`drizzle-orm`): snapshots live table → archive (never deletes), then backfills a collapsed transition log keeping FIRST occurrence per `(fingerprint, decision, reason_code)`. **Idempotent + dry-run default** — owner-run, not in any runtime path. Writer (`logUpstreamReasoningBatch`, 30s tick) now write-once-per-transition: `selectUpstreamRowsToWrite` is **PURE** (no map mutation; collapses within-batch dups via a local `seenInBatch` Set keyed `identity\u0000stateKey`); `logFnoReasoning` returns `Promise<boolean>` (true insert ok, false swallowed failure — non-throwing contract preserved); `commitUpstreamDedupeState` advances the per-identity `upstreamLastState` map ONLY after a write returns true → **fail-OPEN**: a failed write leaves state un-advanced and is retried next cycle (would rather write one duplicate than silently drop a transition). Hydrates latest-state-per-fingerprint-today on first batch after boot so a restart doesn't re-dup; upstream-decision-scoped. Downstream OPENED/CLOSED/SKIPPED already deduped — untouched. Tests in `fnoSignalReasoningLogger.test.ts` cover identity/state-key normalisation, repeated→1 write, changed reason→new row, within-batch collapse, no-commit-no-advance (fail-open retry), and the 2–6 rows/signal target.
- **Tier A3 — win-rate zero-denominator honesty**: pure exported `foWinRate(wins, losses): number | null` in `paperAnalyticsFO.ts` (zero denominator → `null`, aligning with `reportsView.ts`), used at the summary + per-setup sites; field types widened to `number | null`. `journalAnalytics.ts` `buildAnalytics` exported for tests. Scanner consumers render `"—"` instead of a fake `100%`/`0%`: `paper-reports.tsx` (`wrText`/`wrTone` at all 3 render sites) and `paper-trading.tsx` (`FoAnalytics.winRate` + `bySetup[].winRate` → `number | null`, both render sites null-guarded). Tests: `paperAnalyticsFO.test.ts`, `journalAnalytics.test.ts`.
- **Tier A2 — reconciliation truth-labels (display only, no calc change)**: `options.tsx` carries a one-line "Triggered (signals) vs Closed (trades)" definition — Triggered = `option_signal_history` (market hit the level); Closed = `paper_trade_fo` (system took the risk).
- **Validation**: full `pnpm run typecheck` green; api-server `vitest --pool=threads` 934/934; scanner `vitest --pool=threads` 572/572; archive table pushed. Architect review (includeGitDiff:true) PASS — fail-open semantics confirmed correct, no trading/execution path touched. **Not published** — owner deploys when ready.

### Backtest Lab — F&O backtesting tab (scope C+D, 2026-06-05)
Owner/subscriber tab at `/backtest-lab` (nav: "Backtest Lab", `BACKTEST_LAB` registered in `auth-api.ts` ALLOWED_TAB_KEYS + TAB_PATH, `App.tsx` route, `layout.tsx` nav, `admin.tsx` TAB_LABELS, `users.ts` allow-list). **In-app, runs against real persisted history — completely separate from the in-repo Python `tools/fno-backtester/`.** Two run modes; the live engine (`optionSignals.ts`) and legacy engine are UNTOUCHED and never mixed.
- **HARD RULES upheld (architect-verified PASS)**: no fake/synthetic option data anywhere; no look-ahead (forward-only walk, manage-then-enter per bar, equity curve built in exit-time order); honest unavailable states everywhere ("Historical option data unavailable… skipped (no fabricated data)"); existing features untouched.
- **Tables** (`lib/db/src/schema/backtest.ts` + barrel): `backtest_runs` / `backtest_trades` / `backtest_blocked_setups`, all **ownerKey-scoped** (`owner` vs `u:<userId>`), FK cascade. Pushed via `drizzle-kit push`.
- **Mode A REAL_REPLAY (100% real)**: `lib/backtest/replay.ts` reads `option_signal_history` (real emitted trades + outcomes) + `fno_signal_reasoning` (blocked setups + audit) + `iv_history` over a window that grows daily. Option exits computed ONLY from real captured fields (`option_stop_loss`/`option_target1`/`option_target2`); undecided trades stay `pnl=null`/`optionExit=null` (no fabricated fills). Honest decided/total split (smoke: 18 decided / 82 total).
- **Mode B DIRECTIONAL (2yr, modeled)**: `lib/backtest/directional.ts` replays the reconstructable directional layer (regime/EMA/VWAP/RSI/ATR) on historical 15-min index SPOT candles, reusing the REAL pure libs (`indicators.ts`, `regimeClassifier.ts`, `projectOptionLevel`). Option P&L via a **clearly-LABELED delta proxy** — every trade carries `modeled: true`, `optionEntry`/`optionExit` stay null, and `buildDirectionalDataQuality` flags every modeled field. Candle source `lib/backtest/candleSource.ts` (smoke: 551 modeled trades + equity curve).
- **Mode D coverage**: snapshot-coverage panel (earliest/latest/count) surfaced in Data-Quality. Retention extended so history accrues for a future faithful replay: `optionChainSnapshotIngestor.ts` default `30→825` days, max clamp `365→1100`. Retention-only change; ingestion semantics unchanged.
- **Routes** (`routes/backtest.ts`, mounted in `routes/index.ts`): `requireSubscriberOrOwner("BACKTEST_LAB")`, ownerKey-scoped on every query (list/get/delete/trades/blocked). POST run / GET runs / GET runs/:id / GET runs/:id/trades / GET runs/:id/blocked / GET snapshot-coverage. Zod-validated against generated schemas. Express-5 `req.params` narrowed via `paramId(req)` helper. **CSV export is client-side** (no export endpoint/hook) — DIRECTIONAL rows always carry `modeled=true` + blank option premiums.
- **OpenAPI**: `/api/backtest/fno/*` paths + `BacktestRun*`/`BacktestTrade`/`BacktestSummary`/`BacktestBlocked*`/`BacktestSnapshotCoverage` DTOs in `lib/api-spec/openapi.yaml` → generated hooks (`useListBacktestRuns`/`useCreateBacktestRun`/`useGetBacktestRun`/`useGetBacktestRunTrades`/`useGetBacktestRunBlocked`/`useGetBacktestSnapshotCoverage`/`useDeleteBacktestRun`). Generated query hooks require an explicit `queryKey` in `options.query` — pass the matching `getGet*QueryKey(...)`.
- **Frontend** (`pages/backtest-lab.tsx`): mode switch + filters (instrument/period/timeframe/capital/risk), summary stats, equity-curve AreaChart + P&L-distribution BarChart (Recharts via `ChartContainer`), trades table, blocked-setups table (REAL_REPLAY only), Data-Quality panel, client-side CSV export, honest n/a states + modeled badges.
- **Validation**: full `pnpm run typecheck` green; scanner `vitest` 572/572; api-server `vitest --pool=threads` 949/949 (incl. retention-default test updated 30→825); backtest unit tests `summary/replay/directional.test.ts` 15/15. Architect review (includeGitDiff:true) **PASS** — no blocking correctness/security/honesty issues. **Not published** — owner deploys when ready.

## Product

- Market scanning (NSE/BSE)
- Advanced options chain analysis (Black-Scholes, Greeks, PCR, Max Pain)
- F&O intraday signals
- Stock-specific catalyst tracking
- Secure user authentication with role-based access
- Paper trading for F&O and equities
- P&L reports and journal analytics
- Global multi-asset scanning (Crypto, Commodities, Forex, Global Equities/Indices)

## User preferences

I prefer clear and concise communication. For coding, I favor functional programming paradigms where applicable. I expect an iterative development approach with regular updates on progress. Please ask for confirmation before implementing any major architectural changes or feature deprecations. Ensure that all new features are accompanied by appropriate tests and documentation. I prefer detailed explanations for complex logic or design decisions.

## Security hygiene

- **Kite token encryption-at-rest** — `kite_session.{api_key, access_token, public_token}` are AES-256-GCM encrypted (envelope `v1:<iv>:<tag>:<ct>`) when `KITE_TOKEN_ENC_KEY` is set. Implementation: `lib/kiteCrypto.ts` + `kiteAuth.ts`. Falls back to plaintext + one-time warn if the key is unset — **always set the key in production**. Decrypt failures are fail-closed; the daily 06:00 IST login flow recovers. See `docs/audit-implementation-status-2026-05-15.md` (Priority 1) for full notes.
- **Kite-token key rotation** — `pnpm --filter @workspace/api-server run rotate:kite-key` (dry-run) / `... -- --apply` (write). Reads `KITE_TOKEN_ENC_KEY_OLD` + `KITE_TOKEN_ENC_KEY_NEW` from env, decrypts the live `kite_session` row with OLD and re-encrypts under NEW inside one transaction. Never logs token plaintext or key bytes (only sha256[:8] fingerprints). Full operator runbook: `docs/kite-token-enc-key-rotation.md`.
- **DB exports** — never share a raw `pg_dump` of the live DB. Use `scripts/safe-db-export.sh` (excludes `kite_session`; scrubs `users.password_hash` + `global_screener_presets.share_token`).
- **`/api/kite/export-session` is APP_ACCESS_PASSWORD-gated**, not owner-cookie-gated, so dev can mirror prod via `autoMirrorSession()`. If APP_ACCESS_PASSWORD ever leaks, rotate it before the next 06:00 IST. Set `KITE_MIRROR_ALLOWED_HOSTS` explicitly in production.
- **Credential rotation order after a leak**: (1) `KITE_API_SECRET` in Zerodha developer console → (2) `APP_ACCESS_PASSWORD` → (3) `SESSION_SECRET` → (4) Postgres password / `DATABASE_URL` → (5) `TRADINGVIEW_WEBHOOK_SECRET`. Current `kite_session` row auto-dies at the next 06:00 IST or via `DELETE FROM kite_session;`.
- **`.gitignore` blocks** `.env*`, `*.sql`, `*.dump`, `*.backup`, `safe-export-*.sql`. Live security checks live in `securityAudit.ts` and surface in the owner-only audit dashboard.

## Gotchas

- **Kite API Rate Limiting**: Use exponential backoff; do not poll F&O Top-50 faster than 15s.
- **F&O Signal Sweep Cadence**: `TRIGGER_SWEEP_INTERVAL_MS = 30s`. Do not set below 15s (Kite throttling).
- **F&O Signal Pipeline (HARD-CUT 2026-05-06)**: F&O code paths NEVER touch Yahoo. `optionSignals.ts` daily history → Kite `day` interval; `optionSignalGates.ts` VIX → Kite-only (no-op when missing). `tradingConfig.isActionableForFno` returns false for `DELAYED_YAHOO`. Yahoo remains only for non-F&O segments.
- **Paper-trade DB column names**: `paper_trade_eq` uses `entry_price`/`stop_price` (not `entry`/`stop_loss`); `paper_trade_fo` uses `entry_premium`/`stop_premium`. Heat-SQL fragments in `paperAccount.ts` must reference the schema columns exactly — column-name typos surface as `Failed query` + a "continuing" warn that silently skips trade opens (see 2026-05-13 fix).
- **Paper Trading Anti-phantom-trade rules**: Logic for closing already-exited signals and backfilling missing trades. `reconcileMissingPaperTrades` reads `h.tier AS persisted_tier` to prevent post-deploy re-promotion.
- **MissedSignal Log Dedup**: `recordMissedSignal()` returns a boolean to prevent log spam.
- **Signal Display vs. Enum**: When changing display text, only modify the display layer (e.g., `signal-badge.tsx`), never the underlying `Signal` enum strings used by API/DB.
- **OI Change Calculation**: Dependent on `oi_day_low`/`oi_day_high` from Kite quotes (occasional inconsistencies).
- **Indices Board Hang Protection**: `getIndicesBoard()` uses an 8s `Promise.race` deadline; client `<IndicesBoard>` has a 25s `loadingTooLong` flag.
- **Empty-state Copy**: Empty panels show "No live data — last attempt X" + Retry, distinct from a perpetual loading state.
- **Index-detail Pivots**: `/api/index/:slug` returns `previousHigh`, `previousLow`, and classical floor `pivots`.
- **Side-correct R/S in OI analytics**: `computeAnalytics()` filters `topResistance` to CE strikes ≥ spot, `topSupport` to PE strikes ≤ spot.
- **Paper-trade LTP is live**: `GET /paper/positions/fo` and `POST /paper/positions/fo/:id/close` refresh `lastPremium` from a fresh `fetchOptionChain()` pull.
- **Buy column gating**: Scanner `Row` Buy pill renders only when `signal === "STRONG_BUY" || "BUY"`; everything else shows muted `—`.

## Pointers

- **Zerodha Kite Connect**: https://kite.trade/docs/connect/v3/
- **Drizzle ORM**: https://orm.drizzle.team/docs/overview
- **Zod**: https://zod.dev/
- **TanStack Query**: https://tanstack.com/query/latest
- **OpenAPI Specification**: https://swagger.io/specification/

### Backtest Lab V2 — multi-strategy research (2026-06-05)
Research-only extension of `/backtest-lab` (owner OR subscriber). **Never publishes; never touches `optionSignals.ts` or any live engine gate.** Adds a 3-way **Backtest Mode** selector driven by an additive nullable `backtest_mode` enum (`OFFICIAL_ENGINE`/`STRATEGY_RESEARCH`/`COMPARE_OFFICIAL_VS_STRATEGIES`); pre-V2 runs have `backtest_mode = null` (legacy) and render as the official engine.
- **OFFICIAL_ENGINE** keeps the existing two sub-modes (`mode` = `REAL_REPLAY` 100%-real captured signals, or `DIRECTIONAL` directional layer on real 15-min SPOT candles). Strategy modes set `mode = "DIRECTIONAL"` because the strategy registry replays the same real spot candles.
- **Strategy registry** — `artifacts/api-server/src/lib/backtest/strategies/` (`base.ts` types/`Direction = "BULL"|"BEAR"`, `context.ts` precomputed per-bar series + sessions/OR/CPR/prev-day, `filters.ts`, `registry.ts`, 6 generic modules, `runner.ts` walk-forward scale-out harness with **no look-ahead** — causal bar walk only, `comparison.ts` pure multi-factor compare+ranking, `index.ts` barrel). `getStrategy(id)`/`listStrategies()`. Runner emits per-trade `direction: "LONG"|"SHORT"` (distinct from the official engine's `"BULLISH"|"BEARISH"`) — the frontend's `isBullishDirection()` treats `BULLISH`/`BULL`/`LONG` as bullish so strategy trades render with the correct CE/colour. **Option P&L is a clearly-flagged delta proxy (ATM Δ≈0.5) over an equal-weighted session-mean VWAP proxy; premiums stay blank, `modeled=true`.**
- **Confirmation filters** (`BacktestFilterConfig`): `vwapFilter`/`emaTrendFilter`/`avoidChopZone`/`avoidLast15Minutes`/`minimumRiskReward` are user-toggleable; **`optionChainConfirmation`/`avoidWideSpread`/`avoidLowVolume` are force-disabled server-side** (no historical option/spread/volume data exists) and shown as "auto-disabled" in the UI — never silently applied. Server `DEFAULT_FILTERS`: vwap/ema/chop/last15 true, minRR 1.5.
- **Multi-factor comparison/ranking** (`buildComparison` → `BacktestStrategyComparison{rows, byStrategy, ranking, notes}`): per-(strategy × index) rows (gross/charges/slippage/net, win%, PF, avgR, maxDD, T1/T2/SL/Time exits, rejected/data/risk-blocked counts), per-strategy aggregates with a composite score, and ranking cards (BEST_OVERALL/HIGHEST_WIN_RATE/BEST_PROFIT_FACTOR/LOWEST_DRAWDOWN/MOST_CONSISTENT). **Ranking is a multi-factor composite — never net-profit-only**; too-few-trades strategies are `eligible=false` and not ranked. Charges/slippage are modeled estimates (toggle `includeCharges`/`includeSlippage`). In COMPARE mode the official engine is added as a comparison unit tagged `OFFICIAL_STRATEGY_ID="OFFICIAL_ENGINE"` (shows an "engine" badge) — this strategy-field tagging on official rows is **intentional** so the engine ranks alongside strategies; standalone OFFICIAL_ENGINE runs carry `signalSource="ENGINE"`.
- **Per-trade / per-blocked attribution** — additive nullable cols on `backtest_trades` (`backtest_mode`, `strategy_id/name/category`, `signal_source`, `strategy_params`, `confirmation_filters`, `strategy_confidence`, `historical_setup_match`, `passed_conditions`) and `backtest_blocked_setups` (`strategy_id/name`, `category` RISK/DATA/FILTER, `reason_code`, `failed_condition`, `blocked_rule`). Run-level `backtest_runs` gains `backtest_mode`/`selected_strategies`/`filters`/`strategy_comparison` (all nullable; filters null for OFFICIAL_ENGINE).
- **API**: `BacktestRunRequest` extended (`backtestMode`, `strategies[]`, `filters`, `maxTradesPerDay`, `includeCharges`, `includeSlippage`); new `GET /api/backtest/fno/strategies` returns the registry meta catalog; trades/blocked GETs surface all attribution fields. OpenAPI source-of-truth + codegen regenerated.
- **Honest states everywhere**: no captured option exit → trade excluded from P&L (`pnl=null`, "n/a", never fabricated); modeled fills flagged; strategy catalog/data-unavailable states explicit. **DEFERRED / OUT OF SCOPE**: Apply-to-Paper endpoints, `active_paper_strategies` table, paper-trading UI. Validation: full typecheck green; scanner 572/572; api-server 949/949 (`--pool=threads`). **Not published.**
- **V2 hardening round (2026-06-05)** — three architect-flagged gaps closed: (1) **Advanced per-strategy params are live end-to-end** — `base.ts` exports `StrategyParams`/`resolveParams`/`paramNum` and modules read tunables via `paramNum` (all 6 expose `target1R`/`target2R`; range-reversal also `rsiOversold`/`rsiOverbought`). Frontend `AdvancedParamsPanel` renders numeric inputs from each selected strategy's `defaultParams` and sends `strategyParams` (additive `BacktestRunRequest.strategyParams`, keyed by strategy id, numeric-only); route threads it as `paramOverride` → `runStrategy`; only keys present in a strategy's `defaultParams` are honored, everything else ignored. (2) **Ranking enriched** — aggregates now carry `consistency` (mean per-trade net ÷ stdev, null <2 trades) and `dataQuality` (executed ÷ (executed+data-blocked), null when no opportunities); the composite is reweighted to include both (`{net .25, pf .2, win .15, dd .15, r .1, cons .1, dq .05}`) so it stays multi-factor — never net-profit-only; ranking adds a `MOST_STABLE` card plus `BEST_TIMEFRAME`/`WORST_TIMEFRAME` cards (only when >1 timeframe). UI aggregate table gains Cons./Data columns (honest `n/a` when null). (3) **Block attribution split** — `filters.ts` `FilterRejection.category` distinguishes `FILTER` vs `DATA` (null indicator series — VWAP/EMA/ADX — now emit explicit `DATA` blocks instead of silently passing); runner's `recordBlock` emits `MAX_TRADES_PER_DAY` as `RISK` and routes filter/data failures by `rej.category`, surfaced in the per-row rejected/data/risk counts. Architect re-review: PASS (no look-ahead, `optionSignals.ts` untouched, additive-only, honest states). Validation re-run: full typecheck green; scanner 572/572; api-server 949/949 (`--pool=threads`). **Still not published.**
- **Backtest Lab timezone / session-validity fix (2026-06-05)** — intraday F&O backtest entry/exit times were rendered OUTSIDE the NSE session (e.g. "07:00 pm") instead of 09:15–15:30 IST. **Root cause**: candles are stored as IST wall-clock encoded in UTC fields (09:15 IST → `09:15:00Z`, see `candleSource.parseIstWallClock`); internal math was correct but emission used `.toISOString()`, which mislabeled the IST clock as a UTC instant, and the frontend then re-applied `Asia/Kolkata` (+05:30) → 13:30 displayed as 19:00 (double-shift). **Fix (additive, zero engine-logic change — `optionSignals.ts` untouched, no look-ahead)**: new canonical `artifacts/api-server/src/lib/backtest/time.ts` — `candleUtcIso(t)` emits a TRUE UTC instant (`t - IST_OFFSET_MS` then `.toISOString()`), `candleIstIso` (explicit `+05:30` label), `istMinuteOfDay`, `isSessionValid` (**time-of-day-only**: 09:15–15:30; weekday intentionally NOT enforced because NSE runs legitimate weekend special sessions, e.g. the Union-Budget live session Sat 2025-02-01 present in the real candle history), `isSessionValidUtcIso`, `SESSION_OPEN_MIN=555`/`SESSION_CLOSE_MIN=930`. `directional.ts` (4 entry/exit emissions) and `strategies/runner.ts` (exit + entryTimeIso) now emit via `candleUtcIso`; the wall-clock `iso` kept only for `dayKey`. **`replay.ts` (REAL_REPLAY) is intentionally left on `.toISOString()` — its rows are real DB signal instants, not wall-clock-encoded candles.** `summary.ts` equity curve consumes `t.exitAt`, so the chart inherits the fix. Frontend `backtest-lab.tsx`: `shortDateTime` appends `" IST"`, per-row off-session ⚠ badges (rose) on entry/exit cells, and a **Session Validity Audit** banner in `TradesTable` (green all-valid / rose with count when off-session). Tests: `time.test.ts` — unit (conversion math `09:15→03:45Z`/`15:30→10:00Z`, round-trip no-double-convert, session boundaries, Saturday special-session accepted) + integration regression loading the real CSV (windowed ~3 months, auto-skips when absent) asserting every directional + strategy trade is in-session. Architect review: PASS. Validation: full typecheck green; scanner 572/572; api-server 956/956 (`--pool=threads`). **Caveat: pre-fix persisted runs keep their mis-emitted (IST-as-UTC) times until re-run — no migration was done.** **Not published.**

### Shared indicator primitives + cache-first scan endpoints (audit improvements, 2026-06-07)
Two audit-driven changes; **zero trading-logic change** (`optionSignals.ts` untouched, live scanner scoring bit-identical) and no publish.
- **Shared indicator lib (`@workspace/indicators`, `lib/indicators/src/index.ts`)**: new composite leaf lib holding the SINGLE source of truth for EMA + the (series) Wilder RSI, taken VERBATIM from the trading-critical api-server copy. Re-pointed three modules to re-export from it (`export { ema, rsi }`): `artifacts/api-server/src/lib/indicators.ts`, `artifacts/api-server/src/lib/global/indicators.ts`, `artifacts/scanner/src/lib/charting/indicators.ts`. **Differential analysis is the key result**: the apparent "4× duplication" is only partly real — EMA and series-RSI are byte-identical across the three (safe to unify), but `atr` (api-server EMA-smoothed vs global Wilder RMA), `macd` signal handling (api-server seeds over zero-filled nulls vs global slices from first real value), portfolio scalar `sma`/`rsi14` (flat series → **50** vs series-RSI → **100**), and the three `vwap` variants are GENUINELY DIVERGENT and were deliberately LEFT IN PLACE (unifying them would silently change displayed/derived values). The shared module's doc-comment records exactly what is and isn't unified and why. Wiring: dep + TS project reference added to api-server (dependency), scanner + global (devDependency) and root `tsconfig.json`; `@workspace/indicators` is leaf-only (no circular ref). Golden + identity tests in `artifacts/api-server/src/lib/indicatorsShared.test.ts` lock exact EMA/RSI outputs and assert the api-server re-export IS the shared function reference (no shadow copy). `artifacts/scanner/src/lib/portfolio/indicators.ts` intentionally unchanged.
- **Cache-first scan endpoints (`artifacts/api-server/src/lib/scanner.ts` + `routes/scanner.ts`)**: fixes the up-to-25s block on dashboard views. New `getScanRowsFast()` returns the warm `getCachedScanRows()` snapshot instantly + fires `refreshScanInBackground()` (fire-and-forget, reuses the existing `scanInFlight` dedup); only a COLD cache blocks on `scanAll()` so the very first request still returns real data. Six read-only endpoints swapped from `await scanAll()` to `await getScanRowsFast()`: `/sectors`, `/sectors/:sector`, `/stocks`, `/stocks/:symbol`, `/index/:slug`, `/scan/top`. Boot + interval `scanAll()` left intact. The warm/cold branch is extracted into the pure `selectScanRows(cachedRows, refresh, full)` helper (production behavior identical) and unit-tested in `artifacts/api-server/src/lib/scannerFastPath.test.ts` without touching the live cache or network.
- Architect review: PASS. Validation: full typecheck green; api-server 978/978 (`--pool=threads`); scanner 572/572. **Not published.**

### Backtest run idempotency (2026-06-07)
Re-running byte-identical MODELED backtest inputs now reuses the existing run instead of inserting a duplicate (the only genuine defect from the external "deep audit"; every other audit claim was a stale-CSV / CSV-export-formatting artifact or a misread of intentional honest design). **Zero trading-logic change** (`optionSignals.ts` untouched), additive-only, **not published**.
- **Key** — pure `artifacts/api-server/src/lib/backtest/runKey.ts`: `computeBacktestRunKey()` = sha256 over a canonical, recursively key-sorted projection of every output-affecting input (mode, backtestMode, instrument, timeframe, dates, capital, risk, maxTradesPerDay, charges/slippage flags, **order-independent** strategyIds, filters, strategyParams) PLUS two correctness salts: a candle-CSV **`dataVersion`** fingerprint and a hardcoded **`BACKTEST_ENGINE_VERSION`** constant. **REAL_REPLAY returns `null`** → live, continuously-growing DB data is NEVER deduped. **Bump `BACKTEST_ENGINE_VERSION`** whenever backtest/strategy compute logic changes so identical inputs can never silently reuse a result produced by the OLD engine. Over-specificity is safe (extra fields can only MISS a dedup → harmless fresh run, never a false reuse).
- **Data-version** — `candleSource.candleDataVersion(symbols)` = sorted `${sym}:${size}:${floor(mtimeMs)}` over the supported index CSVs (NIFTY/BANKNIFTY/SENSEX). A `fetch:index-candles` refresh changes the fingerprint → cache correctly invalidates. Never reads file contents; missing files encoded honestly. (Caveat: a same-size+same-mtime content swap could miss invalidation — accepted vs. content-hashing large CSVs every run.)
- **Schema** — additive nullable `run_key` column on `backtest_runs` + `uniqueIndex("backtest_runs_owner_run_key_idx").on(ownerKey, runKey)`. Postgres treats NULLs as distinct, so REAL_REPLAY/legacy (null-key) rows never collide; only modeled runs are deduped. Applied to **dev DB 2026-06-07 via `pnpm --filter @workspace/db exec drizzle-kit push`** (note: the replit.md "DB push" command documents the api-server filter, but `drizzle-kit` actually lives in `@workspace/db`; run it from there). **Run the same push against prod after deploy.**
- **Route** (`POST /backtest/fno/runs`) — cache-FIRST: compute key → `SELECT` existing `(ownerKey, runKey)` → return `{...dto, cached:true}` (200) BEFORE the per-owner cap count-check (a cached hit must never 409). On miss: count-check → run → the run-row + child trade/blocked inserts are wrapped in **one `db.transaction`** with `onConflictDoNothing(target:[ownerKey,runKey])`; on the conflict (race-loss) path the loser's insert blocks on the unique index until the winner COMMITS (with all children), so the in-tx winner fetch always returns a complete row as `{...dto, cached:true}` (200). Fresh insert → `{...dto, cached:false}` (201). (Pre-existing non-transactional per-owner cap-count race is unchanged — out of idempotency scope.)
- **DTO/OpenAPI** — `runToDto` gains `runKey`; responses carry `cached`. OpenAPI `BacktestRun` schema adds nullable `runKey` + boolean `cached`; client+zod regenerated. Frontend `backtest-lab.tsx` shows an honest sky-toned "these inputs match an earlier run, result reused (no duplicate); change any input or refresh candles to force a fresh run" notice on `cached` hits.
- Architect review: PASS (after closing two flagged gaps — engine-version salt + transactional run/children write). Tests: new `runKey.test.ts` (10) covers determinism, REAL_REPLAY-null, strategy/filter order-independence, data-version + input sensitivity. Validation: full typecheck green; api-server 990/990 (`--pool=threads`); scanner 588/588. **Not published.**

### Charting institutional indicators — FVG / CVD / POC / Liquidity Sweeps (2026-06-07)
Integrated four institutional indicators from an external "Hrishi Associates" vanilla-HTML handoff into the EXISTING React charting tab (`/charting`, `artifacts/scanner/src/pages/charting.tsx`) — the handoff's "no React / standalone module" framing was for a separate deliverable; the actual ask was to add these to the live React chart. EMA/VWAP/RSI already existed. **Pure-frontend, client-side only — zero backend/DB/schema/trading/signal/scheduler/env change** (`optionSignals.ts` and all paper-trader/signal code untouched). Wired to the REAL `/api/chart/candles` feed (Kite→Yahoo via `useGetChartCandles`), NEVER the file's mock feed. **Not published.**
- **Pure indicator math** (`artifacts/scanner/src/lib/charting/indicators.ts`, all index-aligned + honest-null): `cvdProxy(candles)` — Cumulative Volume Delta as a candle-direction×volume PROXY (bar volume signed by close vs open), returns ALL-null when no positive-volume bar exists (it is NOT true tick-level order-flow delta — the feed has no bid/ask aggression data). `volumeProfilePoc(candles, bins=60)` — Point-of-Control as a volume-profile approximation, null on no positive volume or a degenerate/flat price range. `detectFvgs(candles, maxZones=6)` → `FvgZone` — 3-candle bullish/bearish price imbalances. `detectSweeps(candles, lookback=5)` → `SweepMarker` — stop-runs beyond recent highs/lows that reject back. Tests in `indicators.test.ts` cover the honest/unavailable branches for all four.
- **Renderer** (`artifacts/scanner/src/components/charting-chart.tsx`, lightweight-charts **v5.2.0**): new props `cvdSeries / pocPrice / fvgZones / sweepMarkers / showCvd`. CVD renders as its own sub-pane with a DYNAMIC pane index (`nextPane`: RSI→pane 1, CVD→next, so ordering stays correct whether one or both are on); POC as a price line; FVG zones as top/bottom price lines; sweeps as series markers via **`createSeriesMarkers(series, markers)`** (v5 API — NOT `series.setMarkers`); panes via `chart.addSeries(LineSeries, opts, paneIndex)`. The rebuild `useEffect` deps include all new structural props so toggles track correctly.
- **Page wiring / honest states** (`charting.tsx`): four rounded-pill toggles (`toggle-fvg / toggle-cvd / toggle-poc / toggle-sweeps`, off by default) after RSI; series via `useMemo`; **`hasVolume`** gate (`candles.some(c => c.v>0)`) DISABLES the CVD + POC toggles (greyed, `disabled:opacity-40`) and surfaces "needs volume — unavailable on this source" tooltips on null-volume sources (delayed Yahoo / global symbols). CVD pill is labelled `CVD*`; footer copy names all indicators and carries the explicit proxy/approximation disclosure. Architect review: charting implementation PASS (its "scope violation" flag was a known `includeGitDiff` false-positive reading PRIOR-merged backtest commits — working tree is charting-only, 4 files). Validation: full typecheck green; scanner 601/601.

### Charting pro-grade upgrade — Fibonacci / Fixed Volume Profile / S-R confluence (2026-06-07)
Made the EXISTING `/charting` tab (`artifacts/scanner/src/pages/charting.tsx`) feel professional-grade like Zerodha Kite, building on the FVG/CVD/POC/Sweeps work above. **Pure-frontend, client-side only — zero backend/DB/schema/trading/signal/scheduler/env change.** Wired to the REAL `/api/chart/candles` (Kite→Yahoo via `useGetChartCandles`) and the EXISTING `useGetOptionAnalytics` endpoint (fetched ONLY for F&O underlyings, lazily enabled when S/R is toggled). **Honest states everywhere — nothing fabricated.** **Not published.**
- **Declutter + viewport**: default view is clean candles + volume with EVERY overlay/oscillator OFF (EMA/VWAP/RSI/FVG/CVD/POC/Sweeps/Fib/VP/S-R) but all toggle pills still visible. Sweep markers reduced to plain arrows (no text). Chart is viewport-responsive ~70vh, clamped min 560 / max 900 px (was a fixed 480), via a `clampHeight` helper + resize effect; loading/error/empty placeholders share the same dynamic height. `rightOffset:8` / `barSpacing:8` give right-edge breathing room.
- **Pure indicator math** (`artifacts/scanner/src/lib/charting/indicators.ts`, all honest-null): `sliceByWindow(candles, window)` + `VP_WINDOWS` (`ALL/1Y/6M/1M/1W/1D`); `fibLevels(candles)` → `FibResult` (auto dominant-swing retracement 0→1.0 + 1.272/1.618 extensions; null on degenerate range); `fixedVolumeProfile(candles, opts)` → POC/VAH/VAL + row histogram over a 70% value area (null on no positive volume or flat range); `swingPivots(candles)` → price-action levels; `computeKeyLevels(candles, currentPrice, optionLevels)` → top-3 Support + top-3 Resistance by clustering Fibonacci + Price-Action + Option-Chain (Put/Call OI) candidates, each level tagged with its backing `sources[]`. Ranking is by confluence score, ties broken by NEAREST-to-price (`byScoreThenNearest`) so the closest strong levels survive the top-N slice; supports relabelled S1..S3 high→low, resistances R1..R3 low→high.
- **Renderer** (`artifacts/scanner/src/components/charting-chart.tsx`, lightweight-charts v5): new props `fibLevels`, `volumeProfile`+`showVolumeProfile`, `keyLevels`+`showKeyLevels`. Volume Profile drawn via a custom `ISeriesPrimitive<Time>` (`VolumeProfilePrimitive`/`PaneView`/`Renderer`) — a left-anchored horizontal histogram (maxBarW = width×0.26) with POC + value-area fills, plus POC/VAH/VAL price lines. Fib levels render as dashed gold/purple price lines; S/R as solid price lines whose title is `${label} · ${sources.join(" + ")}` so each level SHOWS what backs it.
- **Page wiring / honest states**: toggle pills for Fib / Vol Profile (+ window selector) / S-R. Option analytics query `enabled` only when `showKeyLevels && fno` (`findFno`); option S/R derived from `topSupport/topResistance.strike`. `fibResult`/`volumeProfile`/`keyLevelsResult` via `useMemo`; VP disabled on no-volume sources. Footer copy explains Fib is off the dominant swing, VP is a volume-profile approximation needing volume, and S/R clusters Fibonacci + price-action + (F&O) option OI with sources shown — levels appear only with enough data, never fabricated. Architect review: PASS after fixing a support tie-break bug (was preferring farther supports on ties) + adding a nearest-survives-slice regression test; its "scope violation" flag was the known `includeGitDiff` false-positive reading prior-merged backtest commits (working tree is charting-only, 4 files). Validation: full typecheck green; scanner 637/637.

### Charting index-futures VWAP + solid Fib + full-screen + OHLC legend (2026-06-07)
Extended the EXISTING `/charting` tab so VWAP/Volume-Profile become meaningful on SPOT INDICES, made VWAP+Fib unmistakable, and enlarged the surface — building on the FVG/CVD/POC/Fib/VP/S-R work above. **Honest states everywhere — NO fabricated volume; zero trading-logic/signal/scheduler/paper-trader change.** **Not published.**
- **Backend index-futures volume datafeed (read-only, fail-OPEN, isolated)**: new `artifacts/api-server/src/lib/indexFuturesVolume.ts` — `indexFutName(symbol)` maps the canonical index charting symbol → Kite F&O `name` (NIFTY/BANKNIFTY/FINNIFTY/MIDCPNIFTY/NIFTYNXT50/SENSEX/BANKEX; null otherwise), resolves the nearest-expiry FUT instrument token via `loadFnoInstruments` (cached per IST day), and `fetchIndexFuturesVolume()` pulls that contract's candles via `fetchKiteHistoricalByToken(token, "FUT:"+name, interval, daysBack)`, returning `Map<epochSec, volume>` of only strictly-positive real volumes. Wired into `chartDatafeed.tryKite`: for `segment==="index"` it merges futures volume onto the spot index candles by timestamp BEFORE weekly/monthly aggregation (helper `mergeIndexFuturesVolume`). **Fail-OPEN at every layer** — any resolve/fetch failure returns the original candles untouched. **No OpenAPI/codegen change**: spot-index cash volume is always 0, so any positive volume on the Kite index path is unambiguously futures-derived; the client infers the "FUT" label from `segment==="index" && source==="kite" && hasVolume`. This is the TradingView convention (index charts use front-month futures volume for VWAP/VP).
- **Renderer** (`artifacts/scanner/src/components/charting-chart.tsx`): VWAP line is now **SOLID** (`LineStyle.Solid`, was Dashed) with `lastValueVisible`. **Fibonacci** retracements are **SOLID** (was dashed — they were getting lost among gridlines on indices) with the **0.5 / 0.618 golden pocket emphasized** (`lineWidth 2`); extensions are dotted (they sit outside the swing range). New **crosshair-following OHLC legend** overlay (top-left, `pointer-events-none`, `data-testid="chart-ohlc-legend"`): reads O/H/L/C + absolute & % change straight from the supplied candle data (works for candle AND line types), defaults to the last bar, follows the crosshair via `subscribeCrosshairMove`. **XSS-safe**: `innerHTML` content is numeric-only (`toLocaleString`/`toFixed`) plus fixed labels/colors — no user-controlled HTML.
- **Page wiring** (`artifacts/scanner/src/pages/charting.tsx`): `hasVolume` memo MOVED above `vwapSeries` (avoids a TDZ ReferenceError) and the VWAP memo + toggle are gated on `showVwap && intraday && hasVolume` — so indices with FUT volume ENABLE VWAP, while volume-less sources DISABLE it with a branching tooltip (mirrors the existing CVD/POC/VP honesty). New **full-screen** toggle (lucide `Maximize2`/`Minimize2`, `data-testid="toggle-fullscreen"`): a `fixed inset-0 z-50 overflow-auto` overlay with Escape-to-exit; `clampHeight` bumped to ~78vh (min 600 / max 1100, was 70vh / 560–900) and an `effectiveHeight` (`viewportH − 132` when full-screen) feeds the chart + all loading/error/empty placeholders. Honest header badge **"Vol · {SYM} FUT"** (`data-testid="badge-futures-volume"`) shown only when `segment==="index" && source==="kite" && hasVolume`, with an explanatory tooltip. Footnote expanded to explain the index→futures-volume VWAP convention (and that VWAP is honestly disabled with no real volume) + the new solid-Fib / golden-pocket hierarchy.
- **Tests**: new `artifacts/api-server/src/lib/indexFuturesVolume.test.ts` (mapping only, no network — supported symbols, case-insensitivity, null for no-future indices). Validation: api-server 999/999, scanner 637/637, full typecheck green. Architect review: PASS (honest states, no TDZ, XSS-safe legend, read-only/isolated backend confirmed; suggested optional follow-up tests for `mergeIndexFuturesVolume` branches + toggle-disabled states — not blocking).
- **Responsive header fix (2026-06-07)**: the chart-header badge row (symbol + name + source/fresh/FUT badges + full-screen button) was overflowing/clipping the "Full screen" button at narrower viewports. Fixed by letting the name truncate (`min-w-0 flex-1`), making the badge group wrap (`flex-wrap justify-end`, `shrink-0` on chips/button), and collapsing the full-screen button to icon-only below the `sm` breakpoint (`<span className="hidden sm:inline">`). Pure CSS/JSX; scanner typecheck green.
- **Fit-to-screen chart height (2026-06-07)**: replaced the fixed ~78vh `clampHeight` chart sizing (which let the chart + time axis fall below the fold and require scrolling) with a measured fit-to-screen height. A `useLayoutEffect` reads the chart card's live `getBoundingClientRect().top` and sets the chart height to `window.innerHeight − top − BOTTOM_GAP_PX` (min `MIN_CHART_PX=340`), re-measuring on resize and when full-screen toggles. The toolbar/header heights are absorbed automatically because we measure the live top offset, so the whole chart + X-axis is visible without scrolling (Kite-style), in both normal and full-screen modes. `clampHeight` retained only as the SSR/first-paint fallback. Pure CSS/layout; scanner typecheck green.
- **Fit-to-screen ResizeObserver loop fix (2026-06-07)**: the fit-to-screen remeasure originally observed the page ROOT, which CONTAINS the chart whose height it sets — so each height change resized the root and re-triggered the observer, throwing the benign-but-fatal "ResizeObserver loop completed with undelivered notifications" (surfaced as Vite's `[plugin:runtime-error-plugin] (unknown runtime error)` overlay with an empty stack). Fix: observe ONLY the chrome ABOVE the chart (the toolbar `Card` via `toolbarRef` + the header/badges `div` via `headerRef`), never the root. Those nodes are unaffected by the chart's own height, so toolbar wrap / header truncation / async badge changes still trigger a remeasure with zero feedback. Remeasure stays rAF-batched + sub-pixel-guarded. Applies in BOTH normal and full-screen modes (effect re-runs on `isFullscreen`). Scanner typecheck green.
