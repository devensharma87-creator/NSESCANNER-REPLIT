R0.1–R0.3 executed exactly to standard — stopped on evidence, changed nothing, reported factually. And the findings are significant: the landmine is confirmed armed, and it turns out the 4 tables have survived every merge so far by coincidence (drizzle-kit's TTY prompt aborting in non-interactive mode). Coincidence is not a safety mechanism. Rulings on both open decisions, then the missing R0.4–R0.9:
Ruling A — DROP landmine: fix by declaration, and do it NOW as an early-pulled M2. The two options aren't equal. Removing the push line from post-merge.sh (option 2) just hides the drift — Replit's publish-time diff also touches prod schema (R0.3 proved it: that's how the 14 columns reached prod), so the undeclared tables remain at risk through a second channel we don't control. Option 1 is the standing principle: push must be a no-op by construction. And critically for bright line #1: writing Drizzle TS declarations that match the live DB byte-for-byte is a code change, not a DDL statement — no ALTER, no CREATE, no data touched. It's exactly what M2 was always scoped to do, and R0 just shrank M2 from 8 drift items to 4 tables with zero constraint drift. So: pull it forward, execute this weekend. Procedure: paste \d for each of the 4 tables from the live DB → write matching Drizzle declarations (types, defaults, indexes, PKs exact) → re-run the dry-run → diff must show ZERO pending statements → checkpoint with the diff pasted. Until that zero-diff checkpoint passes: no merges, no Publish. After it passes, the push line stays in post-merge.sh — a no-op push is the correct permanent state, and DRIFT-P0 lifts. (reconciliation_report: accepted as never-existed here; note it in the repo memory so nobody "restores" it later.)
Ruling B — F-32/F-27/F-37: hold as-deployed through the weekend; behavior memo required before Monday's open; then I ratify or revert. Market's closed, so zero live risk until Monday 09:15 — no panic revert needed. But note what these are: an event blackout gate is a new signal/entry suppressor, added unauthorized, on a platform whose central complaint is "good moves, no signals." That's directionally the worst kind of unreviewed change. The memo must answer, per feature: (1) exact trigger conditions and defaults (what events, what windows, hardcoded or config?); (2) every path by which it can block, demote, or suppress an emission or paper entry; (3) whether each suppression it causes writes a persisted, canonical-taxonomy skip reason (not free text, not in-memory — same standard as everything else; if it suppresses silently, that alone fails it); (4) is there a flag to disable each feature without a code revert; (5) what the cooldown direction-independence change does to dedup behavior (can it now suppress a legitimate opposite-direction signal?). Default posture if the memo shows silent suppression or no kill-switch: flag them OFF before Monday's open (or revert if no flag exists), and re-admit them properly through the sequence later. If they're conservative, logged, and switchable, I'll likely ratify with documentation — but that's decided on the memo, not on charity.
R0.4–R0.9, re-sent in full (paste-ready, with the rulings folded in):

R0.4 — Paste the value of REASONING_WRITER_V2_ENABLED in every environment this repo runs under: workspace env, deployment/publish config, any .env files. Report anywhere it's OFF or absent. (Prod has the columns now, so flag-ON is safe schema-wise — but I want to know what Monday's session will actually run.)
R0.5 — Commit the shared artifacts into the repo under /memory/ so they bind this environment permanently: the PRD, FNO_COMPLETION_MISSION, case_study_2026-07-17, phase0_data_availability_matrix, PROJECT_DELTA_REPORT, and the conversation-record PDF reference. Paste the committed file list. Add one new file: REPLIT_MIGRATION_NOTES.md recording today's R0 findings (reconciliation_report never existed here; publish-time diff mutates prod schema; the 4-table declaration fix; F-32/F-27/F-37 pending ruling).
R0.6 — The F-32/F-27/F-37 behavior memo per Ruling B above. No edits to the features themselves.
R0.7 — Full backend suite + typecheck baseline on this environment: literal pass counts, literal exit codes. This is the number every future checkpoint diffs against.
R0.8 — Kite end-to-end on this environment: complete a login through the new forwarder, verify the session row persisted, KiteReadiness state green, ticker live. Paste evidence.
R0.9 — Candle warehouse coverage here: per-index 15-min row counts and date ranges for NIFTY/BANKNIFTY/SENSEX. If empty or thin, the pre-approved Kite historical backfill (provenance-labeled, off-hours, ~84 calls) executes per the M0-B plan.
Then, same weekend, in order: M2-early per Ruling A (declarations → zero-diff checkpoint) → M0-B study (3 entry models × staleness windows × 90 sessions × 3 indices, descriptive stats only, no ranking) → the ≤2-page trigger-geometry memo.
Standing: no merges/Publish until the zero-diff checkpoint; no feature work until R0 closes; all bright lines from the committed PRD apply.