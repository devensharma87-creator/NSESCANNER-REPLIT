Hold before publish.

Portfolio Analyser Phase 2 appears implementation-complete after the review fixes, but this task touches persistence and schema, so I need the final controlled report before production.

Provide the full:

Portfolio Analyser Phase 2 Persistence and Corrections Final Report

Required confirmations:

1. DB / migration status

Confirm exactly:

What DB tables were added
What columns were added
Whether migration is additive/non-destructive
Whether any existing table/column was modified
Whether any production DB migration has been applied
Whether schema push was dev-only
Production migration steps required before/with publish
Rollback plan if migration fails

Important:

Do not apply production DB migration until explicitly approved.
2. Persistence verification

Confirm:

Create portfolio works
Save portfolio works
Portfolio reloads after browser refresh
Edit holding works
Delete holding works
Clear portfolio and save empty portfolio works
Delete default portfolio promotes/falls back safely
CSV import then save works
Manual add then save works
Default portfolio works
User portfolios are isolated by ownerKey
No portfolio data leaks across users
3. API endpoints

List all endpoints added/changed:

GET
POST
PUT
DELETE

Confirm all are authenticated and ownerKey-scoped.

4. Corrections implemented

Confirm:

Detail drawer blank fields improved
50-DMA/200-DMA mapped from stock detail or candle fallback
Fundamentals show value or proper unavailable reason
ETF fundamentals show not applicable
XIRR/annualised return label corrected
Trend/structure score scale explained
Day P&L formula verified
Dividend + realised P&L included where relevant
NIFTY 50 benchmark added where feasible
NIFTY 500/sector benchmark honestly marked unavailable if not available
Risk analytics added
Sector concentration added
Top-3 concentration added
CSV export added
Bare em-dashes replaced with meaningful unavailable states
Methodology documentation added
5. ETF CMP investigation

Confirm:

NIFTYBEES / GOLDBEES / BANKBEES status
Root cause
Whether simple fix exists
Whether unresolved ETF CMP is clearly labelled
No fake ETF price
6. Files changed

List exact files changed by category:

DB/schema
API server
OpenAPI/codegen
Scanner frontend
Portfolio libs
Portfolio components
Tests
Docs/replit note
7. Tests

Report exact results:

pnpm run typecheck
pnpm --filter @workspace/scanner run test
pnpm --filter @workspace/api-server run test
architect review
browser/runtime check
8. Guardrail confirmation

Confirm:

No trading logic changed
No F&O logic changed
No paper-trading execution changed
No scanner scoring changed
No Kite auth/session logic changed
No env/secret changed
No scheduler changed
No public portfolio exposure
No auth downgrade
No destructive migration
9. Publish readiness

End with:

Safe to publish: YES/NO
Production DB migration required: YES/NO
Rollback risk: LOW/MEDIUM/HIGH
Known limitations

Stop after the report.

Do not publish.
Do not apply production DB migration.