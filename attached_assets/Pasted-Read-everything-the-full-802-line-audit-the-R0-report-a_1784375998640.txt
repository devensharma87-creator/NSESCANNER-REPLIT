Read everything — the full 802-line audit, the R0 report, and the screenshot. This audit is high quality and I'm accepting it as authoritative: it's source-cited, it distinguishes confidence levels honestly, and — decisively — your own Friday screenshots corroborate its biggest claim. The OI Lab expiry chips showed NIFTY 2026-07-21 (Tuesday), BANKNIFTY 2026-07-28 (Tuesday), SENSEX 2026-07-23 (Thursday) — the real contract master. The signal code hardcodes BANKNIFTY=last-Thursday and SENSEX=Tuesday. Your chain pages read truth; your signal engine reads a stale rulebook. That alone would have made every M5 trade suspect.
First: the Saturday alert explained. Signal key 2026-07-18:BANKNIFTY:BASELINE:BULLISH, triggered 16:21 IST on a Saturday, popping an entry alert off Friday's stale data. Root causes are exactly audit §6.2/§6.6 + our P0.1: the signal sweep scheduler has no exchange-session gate (weekend checks are fragmented and duplicated), so it ran on Saturday, evaluated Friday's last bar as "current," emitted a baseline signal dated 2026-07-18, and the UI alerted on it. It's harmless as a trade (INFO_ONLY, paper trade NO) but it's a gift as evidence: a reproducible, on-demand P0.1-class failure — this exact case becomes M1's first regression test. Also note your own screenshot's top banner: "Public access ON — anyone with the URL can browse this site" — that is audit P0-04 live in production right now.
How the audit maps onto what we already knew: roughly half confirms our existing ledger (P0-05 fail-open ≈ our fail-closed doctrine; P1-03 ≈ our F-27/F-37 concerns; P1-10 ≈ Ruling A, already fixed; §6.6 ≈ P0.1; the two-reconciliation split ≈ our data-honesty theme). The genuinely new stop-ships are three: wrong expiry calendar (P0-01), the ledger-identity break with ₹8L observed drift (P0-03), and the public-mode auth bypass + committed secrets (P0-04) — plus swing auto-open provenance bypass (P0-02), which is a lane we'd deferred but which is actively opening paper positions from Yahoo data at impossible fill prices. All four demand action before Monday's open or before M5. Here is the consolidated, point-to-point plan.

C0 — CONTAINMENT DOCKET (execute TODAY, Saturday — every item is owner-pre-approved now):

Disable public mode. Site goes behind owner auth. (P0-04; your screenshot proves it's ON.)
Secrets rotation batch: rotate APP_ACCESS_PASSWORD, the nse DB password (pending since last week — now forced), session secret (invalidate sessions), Telegram token; delete memory/test_credentials.md from the repo (rotation makes git history harmless — no history rewrite needed); regenerate the Kite API secret in the Zerodha console if feasible. Paste evidence: old credentials rejected.
Disable legacy swing auto-open (runEquityPaperTradingTick / the paperTradingEq auto-open path) via flag or hard guard. (P0-02 — it's opening positions from prohibited data at 09:15-open fills.)
Hard-block all F&O auto-open with an explicit named flag (not the current accidental zero). Nothing may open until the expiry service exists. (P0-01/P0-05.)
Session-gate the schedulers minimally: signal sweep + alert emission skip when the exchange-session service says closed — a 10-line interim guard using the existing market calendar, full service comes in M1. Kills the Saturday-alert class today.
Immutable snapshot of the paper DB (accounts, trades, positions, charges, capital events, audit rows) + deployed SHA, per audit §11 — BEFORE any ledger repair. The ₹8L drift is evidence; nobody "fixes" it with a balance update, ever.
Product banner: "Analysis mode — automation suspended — provenance limits apply." Honest and cheap.
Set REASONING_WRITER_V2_ENABLED=1 in workspace env AND artifact.toml deployment env, restart (weekend = allowed window), verify with a probe write. Prod has the columns; Monday must run instrumented — otherwise next week's evidence is V1-blind.
Commit PROJECT_DELTA_REPORT.md (Devendra: re-upload it to the agent) + append today's audit to /memory/ and log C0 in REPLIT_MIGRATION_NOTES.md.

Exit condition (audit's own): no autonomous open possible, sensitive routes authenticated, snapshot exists, secrets rotated.

MISSION v2 — resequenced (audit tickets folded into our phases):
PhaseScopeSourceC0Containment (above) — todayAudit Phase 0 + our pending itemsM1 (Mon–Wed)P0.1 + one exchange-session/calendar service (replaces all fragmented weekend/holiday/IST checks, owns the event calendar that F-32 currently hardcodes, fixes the pre-market n-2 weekend bug) + suppression persistence + Saturday-2026-07-18 regression case + P0.2 API contractOurs + T03, P1-02, P1-07, §6.2/6.6M2a (this weekend, mostly done)Drift: Ruling A ✅ landed; remaining — verify whether this repo's eodReconciliation.ts writes reconciliation_report (audit says yes on the backup; R0 grep said no here — resolve the conflict), ENV-ISOLATION test DBOurs + P1-10M2b — NEW: Contract Identity (~2 sessions)Exact-expiry service from the live contract master; kill weekday arithmetic everywhere (signals, DTE, force-close, IV snapshots, backtest); BLOCKED_CONTRACT_NOT_FOUND on no exact match; CI drift check vs next 8 listed expiriesT01/T02 — hard prerequisite for M5M2c — NEW: Ledger Truth (~2 sessions)Append-only balanced journal; atomic open/close/settle; capital events in the identity; ONE reconciliation engine; versioned effective-dated cost model; read-only reconstruction of the ₹8L drift to its first bad eventT04/T05/T11 — prerequisite for M5's net-P&L acceptance and M6M3 (~2 sessions)PAPER_WRITER discipline + VIX rider + fail-closed ExecutionDecision contract (critical gates: UNKNOWN→BLOCK, per audit's table) + durable net-equity risk latches (kills process-local resets) + F-27 rebuilt durable with persisted skip reasonsOurs + P0-05/T10/T12M4Owner decision (entry model/lifecycle/regime) — memo needs M0-B, which needs the candle backfill: run it tonight (R0.9, pre-approved)UnchangedM5Real TREND_CONTINUATION emitter → first honest trade — now on correct contracts, honest ledger, fail-closed gatesUnchanged, dependencies addedM620-signal evaluation → expectancy reportUnchangedPost-mission batchPresentation truth: scanner grade=weakest input, report scopes/drawdown denominator, options education/payoff fixes, canonical health snapshot, universe eligibility, sector mappingAudit Phase 3: P1-04/05/06/08/09/11, P2-*
Ruling B closed (with memo + audit): F-32 — retained for now (zero effect until Aug 6) but its hardcoded calendar is condemned; it must consume M1's calendar service, with RBI dates verified against the official schedule, before Aug 6 or it flags OFF. F-27 — provisionally ratified but marked non-compliant (process-local, resets on restart, no persisted skip reason); rebuilt to standard inside M3; agent still owes the Jul 15–18 log audit of what it actually suppressed. F-37 — ratified as diagnostic-only; renamed so "gate" stops lying (audit P2-05); wire-or-retire decided in the post-mission swing work.
Honest timeline impact: first trade moves from ~10 sessions to ~13–16 sessions. That's the audit's cost — and it's cheap, because without M2b/M2c the mission would have produced its first trade on a possibly-wrong contract, sized off inflated cash, with gross-masquerading-as-net P&L — i.e., M6's report would have been beautifully instrumented garbage. The audit didn't slow the mission; it saved its conclusion.
On your desk, Devendra: (1) re-upload PROJECT_DELTA_REPORT.md for commit; (2) confirm C0 execution today — especially public-mode-off and the rotation batch, since both are live exposure right now; (3) the M4 decision stays yours once the backfill+study+memo land, unchanged. Paste-ready C0 directive for the agent is the numbered list above, verbatim — it's written to the four-part standard and every item carries its own evidence requirement. Report back with C0 evidence and tonight's backfill result, and Monday opens contained, instrumented, and honest.