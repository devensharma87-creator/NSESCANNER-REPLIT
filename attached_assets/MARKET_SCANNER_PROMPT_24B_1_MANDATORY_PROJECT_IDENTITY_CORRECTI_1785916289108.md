# MARKET SCANNER — PROMPT 24B.1

## Mandatory Project-Identity Correction — Stock Scanner Pro Only

### This instruction supersedes every conflicting project reference

The only project authorized for this task and the remaining roadmap is:

`STOCK SCANNER PRO`

`GLOBAL MULTI ASSET SCANNER` is a separate Replit project. It is completely outside scope.

Any earlier prompt or execution instruction that mentioned the Global app, Global Dashboard, Global Watchlist, Global Instrument Detail, Global Screener, `artifacts/global`, a Global test suite, Global typecheck, Global build, Global bundle or Global screenshots is withdrawn and must not be executed.

Do not interpret “global” as another part of Stock Scanner Pro. Do not combine the two projects for testing, evidence, route coverage or completion percentages.

---

## 1. Mandatory Step 0 — Prove project identity before doing anything else

Perform only read-only checks and report:

1. Replit project/repl name or project identifier visible to the workspace.
2. Repository root and remote repository name, without fetching.
3. Root `package.json` name and workspace package inventory.
4. Production route registry used by Stock Scanner Pro.
5. Main Stock Scanner Pro frontend package/path.
6. Stock Scanner Pro API-server package/path.
7. Current HEAD, branch and working-tree state.

The preflight must conclude with exactly one of:

`PROJECT_IDENTITY_CONFIRMED — STOCK_SCANNER_PRO`

or:

`STOP — WRONG_REPLIT_PROJECT_OR_PROJECT_IDENTITY_UNCONFIRMED`

If the project name is Global Multi Asset Scanner, if the active production app is the Global project, or if identity cannot be proved, stop immediately. Do not inspect, edit, test, build, revert or clean anything.

---

## 2. Authorized scope

Only the following Stock Scanner Pro surfaces are authorized:

- the Stock Scanner Pro Scanner/web application;
- its active production routes and navigation;
- the API server used by Stock Scanner Pro;
- API Zod and API client React packages used by Stock Scanner Pro;
- shared UI/data helpers directly imported by Stock Scanner Pro;
- Stock Scanner Pro audit-evidence files;
- Stock Scanner Pro tests and build configuration.

The functional scope remains:

- Dashboard and Watchlist;
- stock/index/sector detail;
- scanner and screener;
- charting;
- F&O signals, Options, Option Chain and OI Lab;
- swing analysis, signalling and paper-trading views;
- Portfolio Analyser;
- equity/F&O paper trading, history and P&L;
- Backtest Lab;
- Daily Analysis, reports, diagnostics and system health.

Do not invent a route that is not registered in Stock Scanner Pro.

---

## 3. Explicitly prohibited scope

Do not inspect, edit, test, build, screenshot, audit or count any of the following:

- Global Multi Asset Scanner;
- Global Dashboard;
- Global Watchlist;
- Global Instrument Detail;
- Global Screener;
- `artifacts/global` or any equivalent package belonging to the separate project;
- Global app tokens, components, routes or navigation;
- Global app test totals, typechecks, builds, screenshots or bundle sizes.

If such files happen to be present in the same filesystem or repository:

1. classify them `SEPARATE_PROJECT — FROZEN`;
2. do not modify them;
3. do not revert them automatically;
4. do not include them in Stock Scanner Pro acceptance evidence;
5. report any already-existing Global modifications separately as out-of-scope residue requiring owner review.

---

## 4. Correction to Prompt 24B

Execute Prompt 24B only for Stock Scanner Pro, with these mandatory replacements:

### Route matrix

- Enumerate only Stock Scanner Pro registered routes.
- Remove every Global route and component.
- Prove navigation parity only for Stock Scanner Pro.

### Route implementation

- Apply the established semantic tokens, `DataStatePanel`, `ProvenanceBadge`, `PageHeader`, state honesty, accessibility and responsive corrections only to Stock Scanner Pro routes.
- Do not alter another project's UI to satisfy a route-count requirement.

### Visual QA

- Capture only Stock Scanner Pro production route components.
- Use the six required viewports from Prompt 24B.
- Login screens and API-`401`-only pages remain invalid evidence.
- The safe deterministic fixture environment must be owned by and restricted to Stock Scanner Pro development/test mode.

### Performance

- Measure only Stock Scanner Pro client bundles.
- Do not compare or report Global Multi Asset Scanner bundles.

### Tests

- API server full non-DB suite: retain the reported floor of `5,603`, plus legitimate additions.
- Stock Scanner Pro Scanner suite: retain the reported floor of `1,032`, plus legitimate additions.
- Run relevant Pack 2 F&O, Pack 3 swing, Pack 4 security and Pack 5 provider/fundamentals regressions.
- Do not require or report a Global test suite.

### Typechecks

Run only the four Stock Scanner Pro-relevant typechecks:

1. API server;
2. API Zod;
3. API client React;
4. Scanner/web application.

Do not run or count Global Multi Asset Scanner typecheck.

### Production builds

Run only:

1. Stock Scanner Pro API-server production build;
2. Stock Scanner Pro Scanner/web production build.

Do not run or count Global Multi Asset Scanner build.

### Evidence

Use only the Stock Scanner Pro evidence file:

`artifacts/audit-evidence/FAST_TRACK_PACK_6_PROFESSIONAL_UI_UX_REFINEMENT.md`

Add a project-identity section proving that no Global Multi Asset Scanner file was touched or counted.

---

## 5. Handling prior accidental Global work

The prior reports stated that Global app files were changed and tested. Do not silently carry those changes into Stock Scanner Pro acceptance.

During read-only preflight:

1. list every current changed file attributable to Global Multi Asset Scanner;
2. identify whether it belongs to the current Replit project or a separate Replit project;
3. make no further change to it;
4. do not revert, delete or commit it;
5. report it under `OUT_OF_SCOPE_GLOBAL_PROJECT_RESIDUE`.

Continue Stock Scanner Pro work only if the current active Replit project is conclusively Stock Scanner Pro and the Global files can remain untouched.

If the current workspace is actually Global Multi Asset Scanner, stop and ask the owner to switch to the Stock Scanner Pro Replit project. Do not attempt to navigate across projects automatically.

---

## 6. Roadmap boundary

Remain on the approved Stock Scanner Pro roadmap:

1. finish Pack 6 UI/UX route implementation and visual QA for Stock Scanner Pro;
2. provider activation and parity for Stock Scanner Pro;
3. canonical cross-tab data finalization;
4. professional F&O strategy research and qualification;
5. independent `FNO_PAPER_V2` and `SWING_PAPER_V2` cohorts.

Do not begin later phases in this task.

---

## 7. Final acceptance requirements

Pack 6 may be accepted only when:

- project identity is confirmed as Stock Scanner Pro;
- every active Stock Scanner Pro route is classified;
- required Stock Scanner Pro routes are implemented and visually verified;
- the Prompt 24B responsive, accessibility, state-honesty and evidence gates pass;
- only Stock Scanner Pro tests, typechecks, builds and bundles are counted;
- no Global Multi Asset Scanner file was modified during this task.

The final report must begin with:

`PROJECT_IDENTITY_CONFIRMED — STOCK_SCANNER_PRO`

and may return:

`ACCEPT_FAST_TRACK_PACK_6_PROFESSIONAL_UI_UX_REFINEMENT`

only after all corrected Pack 6 gates pass.

Otherwise return one exact blocker verdict. Do not create another task or pack.

The final evidence terminator must remain:

`END_FAST_TRACK_PACK_6_FINAL_ROUTE_COVERAGE_AND_VISUAL_EVIDENCE_CLOSURE`

---

## 8. Required final response format

Return only:

1. Project identity proof
2. Stock Scanner Pro route matrix summary
3. Stock Scanner Pro changes and visual-QA results
4. Tests, four typechecks and two builds
5. Confirmation that Global Multi Asset Scanner was untouched and excluded
6. Git/integrity record
7. Evidence path, SHA-256 and terminator proof
8. Remaining Stock Scanner Pro roadmap status

Do not return an execution diary.
