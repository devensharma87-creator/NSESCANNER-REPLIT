CODER PROMPT — POST-PUBLISH PRODUCTION VERIFICATION: F&O TRADEABLE SIGNAL TELEGRAM ALERTS

The F&O tradeable-signal Telegram alert build has passed dev verification.

Current dev-verified status:

lib/fnoSignalAlerts.ts added
shouldSendFnoTradeAlert() works
buildFnoSignalAlertText() works
alertFnoTradeableSignal() works
alert hook added after openPaperTrade() returns a new paper trade row
/api/alerts/test-fno-trade-signal works
/api/alerts/status includes lastFnoSignalAlert
sample endpoint is owner-only and rate-limited
sample Telegram alert sends
76/76 tests pass
typecheck clean
LLM index fresh
F&O logic/thresholds/risk guards untouched
broker execution disabled

Previous verdict was:

FNO_ALERT_BUILD_NOT_DEPLOYED

Now the app has been published / must be verified in production.

This is production verification only.

Do not add new features.

Do not change F&O logic.

Do not change F&O thresholds.

Do not change F&O scoring.

Do not change paper-trade rules.

Do not change risk guards.

Do not enable broker execution.

Do not place real orders.

Do not add auto-trading.

Do not change swing logic.

Do not use Yahoo/stale/proxy data for trade-grade F&O.

Do not send info-only/baseline/watchlist/suppressed signals as trade alerts.

Do not expose Telegram secrets.

Do not trim or rewrite replit.md.

Verify production F&O tradeable-signal Telegram alerts after publish.

Confirm:

Production is running the latest F&O alert build.
/api/alerts/test-fno-trade-signal works in production.
/api/alerts/status includes lastFnoSignalAlert.
Sample alert is clearly labelled [SAMPLE].
Sample alert creates no paper trade and no real order.
Real F&O tradeable alert fires only after a new paper trade opens.
Baseline/info-only/watchlist/suppressed signals do not alert.
Duplicate/stale old paper rows do not alert.
Broker execution remains disabled.
No real order is placed.
LLM index remains fresh.

Report:

production deployment timestamp
production URL
current commit/hash
API health
frontend health
server start time
environment mode
whether deployment logs confirm latest build
whether alert routes are mounted
whether /api/alerts/status exists
whether /api/alerts/test-fno-trade-signal exists
whether lastFnoSignalAlert appears in production status response

If the build is still not deployed, report:

FNO_ALERT_BUILD_NOT_DEPLOYED

Verify unauthenticated production access:

GET /api/alerts/status
POST /api/alerts/test-fno-trade-signal

Expected:

unauthenticated request rejected
no alert data leaked
no secret leaked

Verify owner authenticated access:

owner can read status
owner can send test F&O alert
immediate second test call is rate-limited/deduped
no Telegram spam

Call production:

POST /api/alerts/test-fno-trade-signal

Verify Telegram message includes:

[SAMPLE]
F&O TRADEABLE SIGNAL
index
direction
option instrument/strike/expiry
entry
stop loss
target
R
confidence if available
data source/as-of if available
Broker execution DISABLED — no order placed
Manual review required. This is not auto-executed.

Verify it does NOT:

create a paper trade
create a real order
say guaranteed profit
say buy now
imply auto-execution
expose secrets

Report:

HTTP status
telegramStatus
whether message arrived
rate-limit result
/api/alerts/status result excluding secrets

Check whether production can naturally observe a real tradeable F&O paper open.

The real alert must fire only when:

F&O engine opens a new paper trade
openPaperTrade() returns a new row
row is fresh, under 5 minutes old
entry/SL/targets/lots/lot size/confidence fields are valid
signal is not stale/replayed from old idempotency row
broker execution is disabled

If a real tradeable paper trade occurs during market hours, verify:

paper trade row created
Telegram alert sent
/api/alerts/status.lastFnoSignalAlert updates
alert message matches the paper trade row
no real broker order placed

If no live tradeable F&O paper trade occurs during the verification window, do not force one unsafely. Report:

NO_LIVE_FNO_TRADEABLE_SIGNAL_OBSERVED

Verify by tests/code inspection/logs that Telegram trade alerts do NOT send for:

baseline/outlook
INFO_ONLY
WATCHLIST_SETUP
suppressed setup
blocked setup
recovery-veto setup
chase-veto setup
stale Kite/no-live-Kite setup
missing option-chain setup
invalid entry/SL/target/lots
old idempotency row after process restart

Do not loosen any existing gate.

Verify:

same exact F&O alert is deduped for at least 30 minutes
dedup key includes signal identity such as date/index/direction/setup
repeated 30-second cycles do not spam Telegram
process restart does not send stale old row due to fresh-open check

Verify alert text is accurate.

Check:

data source shown honestly
stale/proxy/synthetic/sample data not labelled as fresh trade-grade
sample endpoint is clearly sample
broker execution disabled wording present
manual review wording present
no trade is represented as executed

If option-chain or price data is not fresh/trusted, alert must not send.

Confirm:

broker execution disabled
no real order placed
no broker order id from real broker
no F&O logic changed
no thresholds changed
no risk guards changed
no paper-trade rules changed
no Yahoo trade-grade fallback
no swing logic changed
no WhatsApp added
no destructive DB changes

Run:

api-server typecheck
F&O alert tests
alerting tests
F&O reasoning tests
F&O paper risk guard tests
scanner typecheck if frontend touched
scanner tests if frontend touched

Also run:

pnpm --filter @workspace/scripts run index:llm
pnpm --filter @workspace/scripts run index:llm:check

Report exact pass/fail counts.

Final report must include:

Deployment verification.
Auth/rate-limit result.
Sample F&O test alert result.
Real F&O tradeable alert result or reason not observed.
Negative eligibility check.
Dedup check.
Message source-honesty check.
/alerts/status.lastFnoSignalAlert result.
Broker execution status.
Tests run.
LLM index regeneration/check result.
Remaining blocker.
Exact next action.

Final verdict must be exactly one:

FNO_TRADEABLE_ALERTS_PROD_VERIFIED
FNO_TRADEABLE_ALERTS_PROD_VERIFIED_NO_LIVE_SIGNAL_OBSERVED
FNO_ALERT_BUILD_NOT_DEPLOYED
FNO_ALERT_SAMPLE_TEST_FAILED
FNO_ALERT_ELIGIBILITY_BUG_FOUND
FNO_ALERT_SOURCE_HONESTY_BUG_FOUND
FNO_ALERTS_PROVIDER_ERROR
ROLLBACK_REQUIRED

Task is complete only if:

production build is verified
sample alert works in production
sample alert creates no paper/real order
lastFnoSignalAlert exists in production status
real tradeable alert path is verified if naturally observed
negative eligibility is verified
duplicate spam is prevented
broker execution remains disabled
no real order is placed
tests pass
LLM index is fresh
final verdict is explicit

This is production verification only.

Do not tune the F&O engine.

Do not make more signals.

Do not send info-only alerts.

Do not enable broker execution.

Only real, fresh, tradeable paper-open F&O signals can alert.