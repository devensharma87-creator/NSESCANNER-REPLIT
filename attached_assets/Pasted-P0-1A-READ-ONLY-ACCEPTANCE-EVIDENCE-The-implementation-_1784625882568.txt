P0.1A — READ-ONLY ACCEPTANCE EVIDENCE

The implementation and permitted verification have finished.

Do not make further edits. Do not rerun tests or typecheck. Do not connect to a database or external service. Do not commit, publish, deploy, restart, merge or push.

Use only read-only Git and source-file inspection.

Keep the response under 2,500 words.

1. REPOSITORY STATE

Report:

- current branch;
- implementation starting SHA:
  9c0d7b4a4d4553d734e912c3d763ff6a5a42b7e9
- current HEAD;
- main SHA;
- automatic checkpoint SHA/message, if any;
- `git status --short --branch`;
- exact `git diff --name-status` from starting SHA to current HEAD;
- exact `git diff --stat` from starting SHA to current HEAD.

Classify every changed file as:

- test infrastructure;
- package script;
- evidence documentation;
- attachment;
- production/runtime source.

Any production/runtime source change is a stop-condition failure.

2. TRUSTED EXECUTABLE EVIDENCE

Provide file/line references and concise source excerpts proving:

- Vitest package metadata is resolved through Node package resolution;
- the authoritative package `bin` entry is read;
- the real JavaScript CLI is canonicalized;
- `.bin/vitest` is not passed to Node;
- `process.execPath` is canonicalized;
- the CLI must be a regular JavaScript file;
- `path.relative()` is used for package-root containment;
- a CLI outside the canonical Vitest package directory is rejected;
- the spawn executable and complete argument array;
- `shell: false`;
- child PATH is absent;
- a bare `vitest` command cannot be spawned.

State the exact resolved strategy without printing irrelevant machine-specific paths.

3. RUN-ROOT AND CHILD-ENVIRONMENT EVIDENCE

Provide:

- fixed generated-root prefix;
- canonical temporary parent;
- run-root structure;
- permissions used;
- complete child-environment key list;
- complete list of parent variables that may still be inherited.

Prove that parent values for these cannot enter the child:

- PATH;
- HOME;
- TMPDIR;
- TMP;
- TEMP;
- XDG variables;
- NODE_OPTIONS;
- NODE_PATH;
- proxies;
- npm/pnpm configuration;
- operational DATABASE_URL;
- credentials and provider secrets;
- unknown/future variables.

List every production execution-switch override and its exact child value.

Confirm that the old variable:

`TEST_EXTERNAL_SERVICES_MOCKED`

is absent from current test-infrastructure source.

Show the replacement terminology and state clearly that it proves configuration-level disabling only—not runtime network isolation.

4. CLEANUP SAFETY EVIDENCE

Provide file/line references proving:

- `lstat` rejects a symbolic-link run root;
- the target must be a directory;
- temporary parent and run root are canonicalized;
- containment uses `path.relative()`;
- empty, absolute and `..` relative paths are rejected;
- only one direct child of the canonical temporary parent is accepted;
- the basename must have the fixed prefix;
- cleanup does not rely on `startsWith()` path containment;
- cleanup does not rely on `includes(TEST_RUN_ID)`;
- cleanup executes at most once;
- normal close, non-zero exit, child error and synchronous spawn failure are handled;
- validation failure refuses deletion.

State any remaining local filesystem race limitation honestly.

5. HARD DB/APPLICATION TEST LOCK

Provide file/line evidence proving:

- default `test` routes through the preflight runner;
- `test:db` routes through the preflight runner;
- `test:unit` uses only the positive unit allowlist;
- the runtime hard block reason is exactly identified;
- the hard block executes before any real Vitest spawn;
- it cannot be disabled through an ordinary environment variable;
- fake-spawn tests confirm the spawn sentinel is untouched;
- no real DB/application test can currently start.

Include the exact current package scripts.

6. RECORDED VERIFICATION

Do not rerun anything. Report the existing recorded results:

Typecheck:

- exact command;
- exit code;
- warnings/errors.

Unit tests:

- exact command;
- test files collected;
- passed;
- failed;
- skipped;
- timed out;
- duration;
- exit code;
- warnings/stderr;
- whether any real child process was spawned;
- whether any database or network connection occurred;
- whether temporary test roots were cleaned successfully.

7. DOCUMENTATION CONSISTENCY

Confirm whether both existing P0.1 evidence documents were updated.

Report their exact paths and verify that their environment-key counts, test counts and acceptance labels match the current source.

8. FINAL STATUS

Return this exact table:

PURE_HELPER_TESTS
API_SERVER_TYPECHECK
TRUSTED_EXECUTABLE_RESOLUTION
CHILD_PATH_ISOLATION
HOME_FILESYSTEM_ISOLATION
TEMP_FILESYSTEM_ISOLATION
XDG_FILESYSTEM_ISOLATION
CLEANUP_PATH_VALIDATION
CHILD_ENVIRONMENT_POLICY
PRODUCTION_SWITCH_OVERRIDES
EXTERNAL_PROVIDER_CONFIGURATION
EXTERNAL_NETWORK_ISOLATION
DB_TEST_RUNTIME_LOCK
DB_RUNTIME_ISOLATION
APPLICATION_TESTS
PRODUCTION_DB_TOUCHED
EXTERNAL_SERVICE_CALLED
PRODUCTION_SOURCE_CHANGED
WORKFLOW_RESTARTED
DEPLOYED
P0_1A_ACCEPTANCE
P0_1_OVERALL

Expected maximum classifications:

- P0_1A_ACCEPTANCE may be ACCEPTED only if every local containment invariant is evidenced.
- EXTERNAL_NETWORK_ISOLATION remains NOT_EXECUTED.
- DB_RUNTIME_ISOLATION remains NOT_EXECUTED.
- APPLICATION_TESTS remain BLOCKED_PENDING_P0_1B.
- P0_1_OVERALL remains PARTIAL_PENDING_P0_1B.

Stop after this report.