Cleanup verification only. No feature work. No code changes unless explicitly required below. No trading-logic changes.
This task closes two open confirmations from the last session. The --pool=threads fix and strategyControl.ts schema were landed but never verified to completion — the test suite hit the 120s bash ceiling, and the schema audit ran against the dev database, not production. Both need a real, literal result. "Should pass," "matches T007 baseline," and "no-op on dev" are not acceptable closures. I need the actual output lines.

ITEM 1 — GET THE LITERAL api-server PASS LINE
Context: The api-server suite needs ~130s; the agent bash tool kills at 120s. The --pool=threads fix is baked into package.json. The run is healthy — it just needs a window without the 120s ceiling.
1.1 Run the full api-server suite to completion in an environment without the 120s limit (Replit Shell tab, or background the process to a logfile and poll it until it finishes — whatever gets a complete run). Command: pnpm --filter @workspace/api-server run test.
1.2 Paste the literal final summary lines — the Test Files … passed and Tests … passed lines with totals, exactly as printed. Prior baseline: 1908 passed (111 files).
1.3 If the executed test count is lower than 1908, that is a finding, not a pass — list every missing/skipped test and why. If any test fails, paste the failure output in full. Do not summarize a failed or partial run as "healthy."
1.4 If you genuinely cannot get an unbounded run in your environment: say so explicitly, and confirm the suite is configured so that I can run pnpm --filter @workspace/api-server run test in the Replit Shell and get the green line with no extra flags. State that as "blocked, owner-run required" — do not close Item 1 as done.

ITEM 2 — PRODUCTION SCHEMA DRY-RUN (the audit that actually matters)
Context: Last session's schema audit ran against the workspace dev DB (helium/heliumdb), which you proved is not production. The at-risk strategy tables (strategy_definitions, 9 rows; strategy_engine_state, 2 rows) live in production. The strategyControl.ts fix protects them once it's in the schema that production reconciles against — but this has only been verified on dev. The next publish runs drizzle-kit push against production. I need to know it's a clean no-op on production, before that publish, not after.
2.1 Determine how to run a read-only drizzle-kit push dry-run / preview against the production database — from the deployed environment, or a shell with the production DATABASE_URL. State exactly which connection target you used and prove it's production (e.g. the prod-only MANUAL_OVERRIDE MOTHERSON record, or a prod-only table/row that doesn't exist in dev). Do not run it against dev again.
2.2 Paste the full dry-run diff output. Required pass condition: zero drops, zero destructive alters, zero renames, zero column removals across the entire production database — and strategy_definitions + strategy_engine_state present and unflagged.
2.3 If the production dry-run shows any destructive operation on any table, that is the finding this audit exists to catch — list every affected table with its row count, and stop before any real push. Do not apply it. Report it to me with the recreate-the-schema fix you'd propose (same additive approach as strategyControl.ts).
2.4 If you cannot reach the production DB from your environment at all: state that plainly, and give me the exact command to run myself against production (the precise drizzle-kit invocation and which env var must point where), so I can run the dry-run and paste you the diff. Mark Item 2 "blocked, owner-run required" — do not infer production is safe from the dev result.

ABSOLUTE RULES

This is verification. Do not modify code unless Item 2.3 surfaces a real at-risk table requiring an additive schema file (and even then, schema-describe-existing-tables only — no DROP/ALTER/RENAME).
Do not apply a real drizzle-kit push to production. Dry-run/preview only. (If the tool can't dry-run without applying, stop and tell me — do not apply.)
No backfill, sync, or write jobs triggered this session.
No trading-logic changes; replit.md and lockfiles untouched.
Where blocked, give me the exact owner-run command — never infer-and-skip.


FINAL REPORT — must contain:

api-server suite: the literal final pass lines with totals, from a completed run — or "blocked, owner-run required" with the exact command.
Production schema dry-run: proof of which DB you targeted (that it was production), the full diff output, and the explicit statement "zero destructive operations" — or the at-risk table list, or "blocked, owner-run required" with the exact command.
Confirmations: no code changed (or only the additive schema file if 2.3 required it); no push applied to production; no write jobs triggered; replit.md/lockfiles untouched.

Stop after this. Once both items show real green results, Stage 2 is next.