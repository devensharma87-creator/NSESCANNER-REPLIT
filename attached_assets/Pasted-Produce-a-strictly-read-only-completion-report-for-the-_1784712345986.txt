Produce a strictly read-only completion report for the equity validated-fill binding correction.

Do not modify any file, rerun tests or typecheck, restart workflows, inspect live logs, access databases, call providers, create another checkpoint, deploy, push or publish.

Using only existing source, Git metadata and already captured outputs, provide:

Starting SHA, final SHA, branch, Git status and exact git diff --name-status.
Exact definition of EquityFillEvidence, including symbol/instrument, price, provider, quote timestamp and trade-grade fields.
Proof that quote age is calculated internally as decision/server time minus the evidence timestamp. Confirm that callers cannot supply quoteAgeSec.
Rejection behavior for missing, invalid, future, stale and outside-session timestamps.
Exact Phase-B return type showing that an allowed result returns the validated fill evidence.
Exact field mapping from the Phase-B result to the durable equity insert:
validated symbol → inserted symbol;
validated price → inserted entry price;
validated timestamp → stored quote timestamp;
validated provider/provenance → stored provenance.
Proof that the writer cannot insert a separately supplied or recomputed symbol or price after Phase B.
A durable-writer coverage table for Equity MANUAL, AUTO and STAGED lanes.
All 20 new EquityFillEvidence test names and what each proves, including:
price mismatch rejection;
symbol mismatch rejection;
stale/future/invalid timestamp rejection;
exact validated-fill insertion;
zero insert callback on rejection;
one insert callback on valid evidence;
caller-supplied arbitrary age impossible.
Reconcile the reported 66/66 count with the earlier 60/60 and 111/111 results, giving exact commands and test scopes.
Explain every change made to paperTradingFO.ts and prove that F&O remains fail-closed with no semantic relaxation.
Disclose the API-server restart, whether it caused any boot-time database or provider activity, and every other deviation from the task restrictions.
Confirm C0 Equity and C0 F&O remain active, broker execution remains disabled, no historical rows were changed and nothing was deployed.

Finish with these labels:

EQUITY_TIMESTAMP_DERIVED_AGE
EQUITY_VALIDATED_PRICE_BOUND_TO_INSERT
EQUITY_VALIDATED_SYMBOL_BOUND_TO_INSERT
ARBITRARY_AGE_BYPASS_ABSENT
ALL_EQUITY_OPEN_LANES_COVERED
FNO_FAIL_CLOSED_PRESERVED
C0_EQUITY
C0_FNO
BROKER_EXECUTION
HISTORICAL_ROWS_MODIFIED
DEPLOYED
INSTRUCTION_DEVIATIONS

Use PASS, FAIL, ACTIVE, DISABLED, NO, YES or UNKNOWN honestly. Stop after the report.