CODER PROMPT — PRODUCTION VERIFICATION FOR P0-2 ZERO-VOLUME VWAP / VOLUME PROFILE HONESTY FIX

I have published / republished the app.

Run production verification for P0-2 only:

ZERO-VOLUME VWAP / VOLUME PROFILE HONESTY FIX

Current status:

FNO_VWAP_VOLUME_PROFILE_HONESTY_DEV_VERIFIED

Expected final verdict if production checks pass:

FNO_VWAP_VOLUME_PROFILE_HONESTY_PROD_VERIFIED

====================================================
STRICT RULES
====================================================

Do not write new feature code unless production verification finds a real blocker.

Do not start trigger wording fix.
Do not start Charting fixes.
Do not start Portfolio fixes.
Do not start Backtesting Phase 3B.
Do not tune confidence scores.
Do not change detector weights except if required to confirm the already implemented zero-volume fix.
Do not change entry rules.
Do not change exit rules.
Do not change SL/target formulas.
Do not change thresholds.
Do not change paper-trade opening eligibility.
Do not change F&O cost model.
Do not change Telegram.
Do not enable broker execution.
Do not place real orders.
Do not perform destructive migrations.
Do not rewrite historical trades.
Do not ask for owner/admin password.

This is production verification only.

====================================================
PART A — FRESH DEPLOY PROOF
====================================================

Check production:

GET https://marketscannerbydev.in/api/build-info

Confirm:

1. HTTP 200.
2. commitShort is after the P0-2 commit.
3. buildTime is after this publish.
4. bootTime is after this publish.
5. environment = production.
6. all checkpoint markers remain true.
7. no secrets exposed.

Previous accepted production state before P0-2 was:

FNO_COST_MODEL_UNIFICATION_PROD_VERIFIED

Do not mark P0-2 PROD_VERIFIED unless production /api/build-info proves the P0-2 commit is live.

====================================================
PART B — RELEASE INTEGRITY
====================================================

Run:

pnpm --filter @workspace/scripts run verify:release

Expected:

PASS, with only known INFO allowed if already documented.

Confirm:

1. /api/healthz works.
2. /api/data-health/global works.
3. /api/build-info works.
4. frontend markers remain live.
5. Data Parity API remains owner-protected.
6. no secrets exposed.

If verify:release has a real FAIL, stop and report:

RELEASE_INTEGRITY_REGRESSION_FOUND

====================================================
PART C — CODE / MODEL PROOF
====================================================

Confirm production contains the P0-2 fixes:

1. `sessionVwap` returns null/unavailable for zero cumulative volume.
2. `rollingVwap` returns null/unavailable for zero-volume window.
3. `volumeProfile` returns null when total volume <= 0.
4. `confluenceEngine` does not score VWAP if `vwapAvailable=false`.
5. `detectBaselineOutlook` removes fake VWAP vote when VWAP unavailable.
6. `detectVwapReclaim` suppresses when VWAP unavailable.
7. `detectTrendContinuation` does not add fake VWAP confidence driver when VWAP unavailable.
8. `OptionSignal.vwapAvailable` exists in generated API schema/types.
9. No fake VAH / VAL / POC is published when volume profile is unavailable.

Report:

| File | Fix Verified? | Evidence | Verdict |
|---|---:|---|---|
| indicators.ts |
| confluenceEngine.ts |
| optionSignals.ts |
| openapi.yaml / generated types |

====================================================
PART D — PRODUCTION F&O SIGNAL HONESTY CHECK
====================================================

Use production-safe diagnostics/API checks.

For NIFTY, BANKNIFTY, and SENSEX, confirm:

1. Index candle zero-volume condition is detected honestly.
2. VWAP is unavailable/null when volume is unavailable.
3. Signal card/output does not show fake VWAP as HLC3.
4. Volume Profile is unavailable/null when total volume is zero.
5. Signal card/output does not show fake VAH / VAL / POC.
6. No “Spot above VWAP” driver appears when VWAP is unavailable.
7. No “Above POC” / “above value area” driver appears when Volume Profile is unavailable.
8. VWAP Reclaim is not emitted from all-null VWAP.
9. Mean Reversion does not calculate distance from fake VWAP.
10. Volume Breakout suppresses with structural reason, not generic fake condition.

Required table:

| Index | VWAP Available? | VWAP Reason | VP Available? | VP Reason | Fake Levels Published? | Verdict |
|---|---:|---|---:|---|---:|---|
| NIFTY |
| BANKNIFTY |
| SENSEX |

====================================================
PART E — SHADOW / DIAGNOSTIC PROOF
====================================================

Confirm the diagnostic/shadow comparison exists or the report contains the shadow impact section implemented in this phase.

Verify it can show:

1. Legacy candidate behavior.
2. Honest candidate behavior.
3. Candidate count delta.
4. Confidence delta.
5. Detector affected.
6. Reason:
   - VWAP_UNAVAILABLE_ZERO_VOLUME
   - VOLUME_PROFILE_UNAVAILABLE_ZERO_VOLUME
   - INDEX_VOLUME_UNAVAILABLE
   - or equivalent.

Required table:

| Index | Detector | Legacy Behavior | Honest Behavior | Confidence Delta | Reason |
|---|---|---|---|---:|---|

If shadow comparison is not persisted but available through tests/diagnostics/report only, clearly state the limitation.

====================================================
PART F — REGRESSION CHECKS
====================================================

Confirm:

1. Release Integrity remains PROD_VERIFIED.
2. P0-1 F&O Cost Model Unification remains PROD_VERIFIED.
3. Backtest Charges Model + Net P&L remains PROD_VERIFIED.
4. Checkpoint 1 remains PROD_VERIFIED.
5. Checkpoint 2 remains PROD_VERIFIED.
6. Checkpoint 2.5 remains PROD_VERIFIED.
7. Checkpoint 3 remains PROD_VERIFIED.
8. Data Parity compat remains PROD_VERIFIED.
9. Provider import guard remains green.
10. F&O cost model guard remains green.
11. Broker execution remains disabled.
12. No real orders placed.
13. No Telegram spam.
14. No strategy threshold tuning occurred.
15. No destructive migration.
16. No stale/report-grade data can drive live trades.

====================================================
PART G — TESTS
====================================================

Run and report exact counts:

pnpm --filter @workspace/scripts run verify:release

pnpm --filter @workspace/api-server run typecheck

pnpm --filter @workspace/api-server run typecheck:libs

pnpm --filter @workspace/api-server exec vitest run src/lib/*indicator*.test.ts src/lib/*optionSignal*.test.ts src/lib/*confluence*.test.ts src/lib/*fno*.test.ts src/lib/*paper*.test.ts src/lib/*fnoCost*.test.ts src/lib/*backtest*.test.ts src/routes/**/*.test.ts

pnpm --filter @workspace/scanner exec vitest run

pnpm --filter @workspace/scripts run index:llm

pnpm --filter @workspace/scripts run index:llm:check

If any suite times out, split into smaller batches and report exact counts.

====================================================
PART H — REPORT UPDATE
====================================================

Update:

1. FNO_VWAP_VOLUME_PROFILE_HONESTY_REPORT.md
2. USER_FACING_CORE_TABS_DEEP_AUDIT_REPORT.md
3. WEBSITE_CANONICAL_DATA_INTEGRATION_REPORT.md

Add production verification section:

P0-2 ZERO-VOLUME VWAP / VOLUME PROFILE HONESTY FIX — PRODUCTION VERIFICATION

Include:

1. Fresh deploy proof.
2. Production commit proof.
3. sessionVwap proof.
4. rollingVwap proof.
5. volumeProfile proof.
6. detector-handling proof.
7. signal-card honesty proof.
8. shadow/diagnostic proof.
9. tests and counts.
10. regression checks.
11. final verdict.

====================================================
FINAL VERDICT
====================================================

Final verdict must be exactly one:

- FNO_VWAP_VOLUME_PROFILE_HONESTY_PROD_VERIFIED
- FNO_VWAP_VOLUME_PROFILE_HONESTY_DEV_VERIFIED
- FNO_VWAP_VOLUME_PROFILE_HONESTY_PARTIAL_GAP_REMAINS
- RELEASE_INTEGRITY_REGRESSION_FOUND
- ROLLBACK_REQUIRED

Use:

FNO_VWAP_VOLUME_PROFILE_HONESTY_PROD_VERIFIED

only if production shows the P0-2 commit live, fake VWAP/VP values are no longer published, diagnostics/reasons are honest, tests pass, and no regressions are found.

Use:

FNO_VWAP_VOLUME_PROFILE_HONESTY_DEV_VERIFIED

if code/tests are green but production still predates the P0-2 commit.

Use:

FNO_VWAP_VOLUME_PROFILE_HONESTY_PARTIAL_GAP_REMAINS

if fake VWAP/VP values can still affect signal confidence or card output.

Use:

RELEASE_INTEGRITY_REGRESSION_FOUND

if verify:release has a real FAIL.

Use:

ROLLBACK_REQUIRED

if secrets leak, broker execution turns on, real orders are placed, Telegram spam occurs, destructive migration happens, or stale/report-grade data can drive live trades.