Start building Phase 1 now: EMA + VWAP + Fibonacci Custom Strategy Builder.

Use Build here, not background. This is safety-critical and needs active supervision.

Strict instruction:

Build quickly, but do not compromise accuracy.
This is not a demo feature. This must behave like a professional F&O strategy engine.

Start only with Phase 1:

EMA rules
EMA 9, 20, 50, 100, 200
Price above/below EMA
EMA crossover
EMA stack
EMA slope
Pullback to EMA
Max distance from EMA filter
VWAP rules
Price above/below VWAP
VWAP reclaim
VWAP rejection
VWAP pullback
Distance from VWAP
VWAP trend confirmation
Fibonacci rules
Auto swing high / swing low
0.382 / 0.5 / 0.618 / 0.786 retracement
1.272 / 1.618 extension target
Entry zone around retracement
Stop below/above swing invalidation

Do not start FVG/SMC or Volume Profile yet. Those are Phase 2 and Phase 3.

Mandatory architecture:

One strategy definition must run identically in:

Backtest Lab
Strategy Research
Official F&O Engine
Strategy Control screen

Do not create separate live/backtest logic.
Do not duplicate rules.
Do not hardcode strategy behavior inside the UI.
The shared evaluator must be the single source of truth.

Mandatory signal-quality gates:

No random trades.
No stale entries.
No future-looking bias.
No repainting.
No trades outside 09:15–15:30 IST.
No fake fills.
No signal unless entry is forward-reachable.
No strategy should pass only because filters were loosened blindly.

Every custom signal must show:

Strategy name
Direction
Entry reason
Which rules passed
Which rules failed
Stop logic
Target logic
Rejection reason if blocked
Whether result is real replay or directional proxy

Backtest requirements:

Run validation on NIFTY, BANKNIFTY, and SENSEX.
Show trade count, win rate, PF, max drawdown, average R, charges/slippage, and rejection reasons.
Proxy P&L must remain clearly labeled as Directional proxy — indicative, not tradeable P&L.

Live-engine requirement:

Custom strategies should be saved and selectable, but default them to disabled for live Official Engine until backtest validation passes.
Add enable/disable control only after the same evaluator is verified.

Testing requirement:

Typecheck must pass.
Existing tests must pass.
Add tests for:

EMA stack/crossover/pullback
VWAP reclaim/rejection
Fibonacci swing detection
Fibonacci retracement entry zone
Same strategy result in backtest and live evaluator
No off-session signal
No stale trigger signal

Bluntly: Phase 1 is the correct fast move.
Trying to implement EMA + VWAP + Fib + SMC + FVG + Volume Profile all together will look productive for 2 hours and then break the engine again.

Professional trader-style build means this order:

Build clean rule engine.
Validate on backtest.
Show reasons clearly.
Only then connect to Official Engine.
Then add FVG/SMC.
Then Volume Profile/TPO last.

Tell the agent: speed is allowed, shortcuts are not.