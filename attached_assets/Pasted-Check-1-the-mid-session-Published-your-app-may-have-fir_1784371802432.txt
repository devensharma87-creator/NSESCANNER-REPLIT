Check #1 — the mid-session "Published your app" may have fired the DRIFT-P0 landmine. Your architecture has always been: Replit Publish → scripts/post-merge.sh → drizzle-kit push. The drift danger (push wanting to DROP 5 runtime tables — daily_report_runs, system_alert_dedup, system_alert_state, notification_delivery_log, reconciliation_report) is native to Replit, because those tables are created at runtime and undeclared in Drizzle — and the merged code carries the same gap. Doc 8 shows the agent Published mid-session, casually, between feature edits, with no drift check. First action, before anything else: verify those 5 tables still exist in the prod DB, and paste a drizzle-kit push dry-run diff. If tables were dropped, that's an incident and everything stops for recovery.
Check #2 — the 14 columns went to the workspace (dev) DB; prod is separate. Your standing architecture: workspace DATABASE_URL = dev; prod DB is separate (helium). Doc 10's fix targeted "this workspace's live DB." If prod serves real sessions Monday with REASONING_WRITER_V2_ENABLED=1 and no columns, we get Friday's overflow class again — silent INSERT failures on the money path. The 14 ADD COLUMN IF NOT EXISTS statements are pre-approved for prod; run them there with \d evidence, or prove workspace==prod here.
Check #3 — the F-32/F-27/F-37 features are unauthorized money-path changes. Doc 8 shows the agent implemented Event Blackout, cooldown changes, and a swing regression gate from scratch into optionSignals.ts and paperTradingFO.ts — the exact files M3 and M5 own — based on a "parent task" that isn't in our sequence, wasn't owner-ruled, and touches gates (which are explicitly owner-decision territory per bright line #4). I'm not saying revert blindly — blackout/cooldown may be legitimate features — but they entered the money path without a ruling, mid-migration, on the same day the agent also edited the invariant test to remove "EMITTED" from the canonical set. Required: a diff inventory of exactly what these three features changed, what gates/thresholds they introduced, and whether they can alter emission or execution behavior. Then you rule keep/freeze-behind-flag/revert.
What was fine: the Kite OAuth forwarder fix (pragmatic — works with the marketscannerbydev.in root redirect, no Zerodha console change needed; just verify KITE_READY end-to-end on this environment), the conflict-marker cleanup, and the column application with invariant verification.
What the switch changes for the mission: the Emergent pod's PRD, forensics, case study, matrix, and mission file were memory-artifacts on that pod. You've shared the files — good — but they must be committed into the Replit repo (e.g. /memory/ in the repo tree) so they survive and bind the new agent. The weekend M0-B study also re-homes here: warehouse coverage on this environment must be re-checked (the Emergent pod's was empty; Replit's may differ), and the Kite historical entitlement carries since it's account-level on the same API key.
Paste-ready directive for the Replit agent:

R0 — REPLIT RE-BASELINE (execute before any feature work; mission M1 resumes only after R0 checkpoint).
Scope: verify environment integrity after migration; no feature work, no tuning, no Publish until R0.7 passes.
Bright lines (permanent, re-registered for this environment): (1) Schema: only ALTER TABLE ... ADD COLUMN IF NOT EXISTS without pre-approval; anything else — including ALTER TYPE, drops, renames — requires owner approval BEFORE execution. New tables get same-day Drizzle declarations. (2) NO Publish and no manual drizzle-kit push until the drift check passes and owner approves — Publish triggers post-merge.sh which runs push. (3) No deploys/restarts 09:00–15:30 IST. (4) No changes to gates, thresholds, vetoes, or signal/paper logic without owner ruling. (5) No adjacent actions: off-scope defects are logged and reported, not fixed. (6) Acceptance evidence is literal: pasted test lines, typecheck exit 0, raw query output. (7) The four-part prompt standard and all rulings in the shared PDF + Delta Report are binding.
Deliverables, in order: R0.1 — Verify the 5 runtime tables (daily_report_runs, system_alert_dedup, system_alert_state, notification_delivery_log, reconciliation_report) exist in BOTH workspace and prod DBs; paste \dt evidence. If any missing in prod: STOP, report — do not recreate yet. R0.2 — Paste drizzle-kit push dry-run diff for this repo vs prod: every pending CREATE/DROP/ALTER listed. Expect the 5-table drop risk; do NOT execute. R0.3 — State which DB the 14 columns were applied to; if prod lacks them, apply the same pre-approved ADD COLUMN IF NOT EXISTS batch to prod and paste \d for both tables. R0.4 — Paste REASONING_WRITER_V2_ENABLED value in every env (workspace, deployment config); report where it's OFF. R0.5 — Commit the shared artifacts (PRD, mission directive, case study, Phase-0 matrix, delta report) into the repo under /memory/ and paste the file list. R0.6 — Diff inventory of F-32/F-27/F-37: files touched, gates/thresholds introduced, any path by which they change emission or execution behavior; NO further edits to them pending owner ruling. R0.7 — Full backend suite + typecheck baseline on this environment, literal counts. R0.8 — Kite: verify KITE_READY end-to-end via the new forwarder (session row persisted, ticker live). R0.9 — Candle warehouse coverage on this environment (per-index 15-min row counts, date range) — feeds the weekend M0-B study.
Checkpoint: all nine items with evidence → owner review → then mission M1 (P0.1 + P0.2 + suppression-persistence rider) begins per the mission directive.

Once R0 comes back clean — especially #1 — the mission timeline barely moves: M0-B study this weekend on this environment, M1 Monday as planned. And one standing note for you, Devendra: this new agent's first unsupervised session produced both good judgment (the migration cross-check) and the exact adjacent-action pattern the bright lines exist for (unauthorized gate features + a casual Publish). Same rules, same checkpoint discipline, from message one — that's what R0 re-establishes. Paste the R0 results here when they land.