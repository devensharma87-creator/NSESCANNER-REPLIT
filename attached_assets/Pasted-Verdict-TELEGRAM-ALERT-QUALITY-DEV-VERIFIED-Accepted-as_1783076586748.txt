Verdict

TELEGRAM_ALERT_QUALITY_DEV_VERIFIED

Accepted as dev/build verified, but not production verified yet.

This is finally the correct type of fix. They found the real structural issue and built the correct foundation:

Old problem:
System alerts used in-memory Map dedup only.

New fix:
DB-backed system_alert_dedup + system_alert_state.

That directly targets the duplicate Telegram problem caused by autoscale restarts and multiple replicas.

What was fixed
Area	Status
Root cause audit	Accepted
New DB-backed dedup layer	Built
New system alert state layer	Built
Did not reuse weak notification_delivery_log	Correct
Warmup failure digest	Built
Per-index F&O warmup spam	Supposedly removed
FNO_DATA_RECOVERED transition tracking	Built
Compact pre-market format	Built
/api/alerts/system-health	Built
Infra Health System Alert card	Built
Backend tests	2,806 passed
Frontend tests	762 passed
Typecheck	Clean
Broker execution	Not touched
Strategy logic	Not touched
Real Telegram sends	Not touched
Why this is not final yet

Because this has not been production-proven.

The real issue was production Telegram spam. So final acceptance requires production proof that:

KITE FINAL WARNING sends once only.
FNO_KITE_SESSION_MISSING sends once only.
FNO_DATA_RECOVERED sends once per real transition only.
F&O DATA WARMUP FAILED becomes one digest, not per-index spam.
Pre-market message is actually shorter and useful.
Multiple workers/cold restarts do not duplicate alerts.
/api/alerts/system-health works in production.
No real trade alert regression.
No broker execution.
No secrets exposed.

So current correct status is:

TELEGRAM_ALERT_QUALITY_DEV_VERIFIED

Expected after publish and live verification:

TELEGRAM_ALERT_QUALITY_PROD_VERIFIED
Important concern

The coder says:

“No real Telegram sends were touched.”

That is good for safety during dev, but it also means the actual Telegram behavior is not yet proven fixed in production.

Unit tests are not enough here because the original problem was caused by production deployment behavior:

autoscale + cold start + multiple replicas + in-memory dedup

So we must test production after publish.

Send this next to coder
CODER PROMPT — PUBLISH + PRODUCTION VERIFY TELEGRAM ALERT QUALITY FIX

Current accepted status:

TELEGRAM_ALERT_QUALITY_DEV_VERIFIED

The implementation is accepted as dev/build verified.

Now publish and run production verification only.

Do not change F&O strategy logic.
Do not change Swing strategy logic.
Do not change thresholds.
Do not enable broker execution.
Do not place real orders.
Do not send test/sample/dev alerts to real Telegram.
Do not send manual test alerts to the real trade channel unless explicitly owner-approved.
Do not trim, rewrite, reorganize, or mention replit.md.
Do not start any new feature.

====================================================
PRODUCTION VERIFICATION OBJECTIVE
====================================================

Verify that the Telegram alert spam and low-value pre-market message issue is fixed in production.

The fix must prove:

1. System/data-health alerts use DB-backed dedup.
2. Multiple workers cannot duplicate the same alert.
3. Cold starts cannot reset dedup.
4. F&O warmup failure is one digest, not per-index spam.
5. Kite final warning sends once per trading day.
6. FNO_KITE_SESSION_MISSING sends once per trading day.
7. FNO_DATA_RECOVERED sends once per proper degraded→recovered transition.
8. Pre-market Telegram is compact and useful.
9. Diagnostics endpoint works.
10. Infra Health System Alert card is live.
11. No trade alert regression.
12. Broker execution remains disabled.

====================================================
PART A — DEPLOYMENT CONFIRMATION
====================================================

Confirm:

- workspace commit
- published production commit
- production boot timestamp
- production process/PID count
- API health
- frontend bundle contains:
  - system alert health card
  - compact pre-market changes
  - /api/alerts/system-health usage if frontend consumes it
- backend bundle contains:
  - systemAlertDedup
  - system_alert_dedup
  - system_alert_state
  - warmup digest logic
  - recovered transition logic

If production does not contain this build, report:

TELEGRAM_ALERT_QUALITY_BUILD_NOT_DEPLOYED

====================================================
PART B — DB SCHEMA VERIFICATION
====================================================

Verify production DB has the new tables:

- system_alert_dedup
- system_alert_state

Check:

1. Tables exist.
2. Required unique/index constraints exist.
3. Insert/upsert is atomic.
4. No destructive migration happened.
5. Existing notification_delivery_log is not misused for system alert atomic claims.
6. No secrets stored in these tables.

Report:

| Table | Exists? | Unique/Constraint | Notes |
|---|---:|---|---|

====================================================
PART C — API VERIFICATION
====================================================

Verify:

GET /api/alerts/system-health

Expected:

- owner-only
- anonymous returns 401
- owner returns 200
- no secrets exposed
- no Telegram bot token
- no Telegram chat ID unless masked
- no DB URL
- no Kite token
- shows alert families
- shows last sent/skipped
- shows duplicate suppressed counts
- shows dedup keys
- shows recovery state
- shows worker/process info if available

Report:

| Check | Result |
|---|---|

====================================================
PART D — SAFE DEDUP SIMULATION
====================================================

Run production-safe verification without spamming real Telegram.

Use dry-run/test mode if implemented.

Verify these alert families:

1. KITE_SESSION_EXPIRED_PREOPEN
2. KITE_FINAL_WARNING
3. FNO_KITE_SESSION_MISSING
4. FNO_DATA_WARMUP_FAILED_DIGEST
5. FNO_DATA_RECOVERED
6. PREMARKET_ANALYSIS scheduled
7. PREMARKET_ANALYSIS manual test

For each:

- first claim should be allowed
- duplicate claim should be skipped
- duplicate across same trading day should be skipped
- recovery should only send on transition
- warmup failure should digest affected indices
- no per-index repeated send

Report:

| Alert Family | First Claim | Duplicate Claim | DB-backed? | Multi-worker Safe? | Telegram Sent? |
|---|---:|---:|---:|---:|---:|

If safe dry-run is not available, verify via unit/integration tests plus DB state, and clearly say live Telegram behavior still requires next natural event.

====================================================
PART E — WARMUP DIGEST VERIFICATION
====================================================

Confirm F&O warmup failure no longer sends one message per index.

Expected new behavior:

One digest:

F&O DATA WARMUP DEGRADED
Affected: NIFTY, BANKNIFTY, SENSEX
Reason: INTRADAY_BARS_MISSING or correct reason
Action: Check diagnostics
No paper trade created
No real order placed
Broker execution disabled

Verify:

1. NIFTY/BANKNIFTY/SENSEX failures are grouped.
2. Same affected set does not resend repeatedly.
3. Different affected set can send only if materially changed.
4. Reason is not UNKNOWN when a known reason exists.
5. Conditions-not-met does not trigger data issue alert.
6. No-signal market condition does not trigger data issue alert.

====================================================
PART F — KITE PRE-OPEN ALERT VERIFICATION
====================================================

Verify:

1. Kite action-required alert max once per trading day.
2. Kite final warning max once per trading day.
3. No duplicate from multiple workers.
4. No duplicate after cold start.
5. No warning on holiday/weekend.
6. Recovery alert once only if configured.

Do not force-spam real Telegram.

Use safe test/dry-run/DB claim simulation where possible.

====================================================
PART G — PRE-MARKET TELEGRAM QUALITY VERIFICATION
====================================================

Generate preview only unless owner explicitly approves a real send.

Verify compact message format.

Expected when Kite missing:

- short action-required message
- Kite missing/expired
- F&O readiness blocked
- Scanner/Paper Trading not trade-grade
- Swing staging summary
- Action: reconnect Kite
- compact footer for unavailable providers
- Broker execution disabled

Expected when Kite active:

- Kite active
- F&O readiness
- signals summary
- key levels available
- option chain available
- swing staging summary
- action links
- compact unavailable-provider footer
- no 20-section essay
- no repeated “Unavailable” blocks in main body

Report before/after sample.

====================================================
PART H — INFRA HEALTH UI VERIFICATION
====================================================

Verify production bundle and, if owner-auth is available, UI:

- System Alert Health card exists
- shows dedup state
- shows duplicate suppressed counts
- shows last sent
- shows last skipped duplicate
- shows recovery state
- shows system alert DB dedup status
- shows Telegram send health
- no secrets exposed

If visual auth is not available, provide exact paths and owner manual checklist.

====================================================
PART I — REGRESSION CHECKS
====================================================

Verify existing systems remain green:

1. Trade alert parity harness.
2. Global data-health endpoint.
3. Swing TTL lifecycle.
4. F&O signal generation unchanged.
5. Swing strategy unchanged.
6. Broker execution disabled.
7. No real orders.
8. No secrets exposed.

====================================================
PART J — TESTS
====================================================

Run and report counts:

pnpm --filter @workspace/api-server run typecheck
pnpm --filter @workspace/api-server run typecheck:libs
pnpm --filter @workspace/api-server exec vitest run src/lib/*alert*.test.ts src/lib/*telegram*.test.ts src/lib/*preMarket*.test.ts src/lib/*fno*.test.ts src/lib/*dataHealth*.test.ts src/lib/*parity*.test.ts src/routes/**/*.test.ts
pnpm --filter @workspace/scanner exec vitest run
pnpm --filter @workspace/scripts run index:llm
pnpm --filter @workspace/scripts run index:llm:check

If full suite times out, run batches and report:

- batch name
- pass count
- fail count
- exit code

====================================================
PART K — REPORT
====================================================

Update:

docs/telegram-alert-quality-audit-2026-07-03.md

or create:

TELEGRAM_ALERT_QUALITY_AND_DEDUP_REPORT.md

Add production verification section with:

1. Deployment evidence.
2. DB schema evidence.
3. Endpoint evidence.
4. Dedup simulation evidence.
5. Warmup digest evidence.
6. Kite pre-open dedup evidence.
7. Pre-market before/after example.
8. Infra Health evidence.
9. Regression checks.
10. Test counts.
11. LLM index status.
12. Remaining limitations.
13. Final verdict.

====================================================
FINAL VERDICT
====================================================

Final verdict must be exactly one:

- TELEGRAM_ALERT_QUALITY_PROD_VERIFIED
- TELEGRAM_ALERT_QUALITY_DEV_VERIFIED
- TELEGRAM_ALERT_QUALITY_PARTIAL_SOURCE_GAP_REMAINS
- TELEGRAM_ALERT_QUALITY_BUILD_NOT_DEPLOYED
- ROLLBACK_REQUIRED

Use TELEGRAM_ALERT_QUALITY_PROD_VERIFIED only if production build is verified and DB-backed dedup is proven live.

Use TELEGRAM_ALERT_QUALITY_PARTIAL_SOURCE_GAP_REMAINS if DB dedup is live but warmup spam, pre-market quality, or duplicate recovery remains unresolved.

Use ROLLBACK_REQUIRED if trade alerts break, secrets leak, broker execution turns on, or real orders are placed.
What you should not do

Do not share the admin password. For UI verification, tell him:

I will visually check owner-only pages myself. Provide exact path and checklist. You complete backend/API/bundle verification and mark UI visual verification as owner-manual pending.
Current status
TELEGRAM_ALERT_QUALITY_DEV_VERIFIED

Next expected:

TELEGRAM_ALERT_QUALITY_PROD_VERIFIED