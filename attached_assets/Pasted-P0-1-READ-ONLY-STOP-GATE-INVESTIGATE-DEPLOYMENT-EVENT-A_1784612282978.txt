P0.1 READ-ONLY STOP-GATE: INVESTIGATE DEPLOYMENT EVENT AND COMPLETE EVIDENCE

This instruction supersedes any unfinished implementation or reporting instruction.

CURRENT DECISION

P0.1 is NOT accepted and remains frozen.

The explicit child-environment allowlist is directionally correct, but the supplied report discloses unresolved PATH, HOME, temporary-filesystem and external-side-effect risks. It is also internally inconsistent and truncated.

MODE: STRICTLY READ-ONLY

Do not:

- edit, create, rename or delete files;
- stage, commit, amend, merge, cherry-pick, rebase, push or switch branches;
- trigger a Replit checkpoint, publish, deploy, rollback or workflow restart;
- stop any deployment or production process;
- execute Vitest, typecheck, build scripts, migrations or application modules;
- import application code;
- connect to any database;
- call Kite, Telegram, Upstox, IndianAPI/INDstocks, Apify or any external service;
- make HTTP requests to the public application;
- change secrets, environment variables, schemas, rows or runtime state.

If Replit automatically creates a checkpoint merely because this instruction was attached, disclose it precisely. Do not amend, revert or conceal it.

Use only static file reads, Git reads and read-only Replit deployment/workflow metadata.

A. MANDATORY BASELINE

Report:

1. Current branch.
2. Current HEAD.
3. main SHA.
4. `git status --short --branch`.
5. All commits after `533ff05fd0992288a5d4cd50f1a019c8de8787fb`.
6. Commit author, committer, author date, commit date, parents, tree and full message for:
   - `533ff05fd0992288a5d4cd50f1a019c8de8787fb`
   - `aeedaef3468aae97c5253d5e5e6cad0613a25493`
7. Tree IDs for both commits.
8. Complete `git diff --name-status` and `git diff --stat` between them.

Do not merely say “identical.” Show the evidence.

B. “PUBLISHED YOUR APP” INCIDENT

Commit `aeedaef3468aae97c5253d5e5e6cad0613a25493` is described as:

“Published your App (auto-commit by Replit platform deploy trigger)”

This occurred while deployment was prohibited. Treat it as an incident until disproved.

Using read-only Replit metadata/logs, report:

1. Whether a publish/deployment event actually started.
2. Event/deployment ID.
3. Initiator: owner, agent, automation or platform.
4. Start and completion timestamps with timezone.
5. Target environment and deployment type.
6. Branch, Git SHA and tree/build identity used.
7. Build ID, release ID and currently served release, if available.
8. Domain(s) affected.
9. Whether any workflow/process restarted.
10. Whether environment variables or deployment configuration changed.
11. Previous deployed build identity and current deployed build identity.
12. Whether the event succeeded, failed, was cancelled or remains active.
13. Exact source of every conclusion: Git evidence, deployment metadata or logs.

Do not stop, roll back or modify the deployment.

If any information is unavailable, write UNKNOWN. Do not infer “no deployment” merely because the Git trees match.

Final incident label must be exactly one of:

- UNAUTHORIZED_DEPLOYMENT_CONFIRMED
- DEPLOYMENT_TRIGGER_WITH_NO_RELEASE_PROVEN
- DEPLOYMENT_STATUS_UNKNOWN

Anything other than fully supported proof remains a blocker.

C. COMPLETE THE TRUNCATED REPORT

The supplied detailed report ends midway through Section 5 at the `REPLIT_DEPLOYMENT` row. Therefore it is incomplete.

Reissue the missing portion beginning with the complete execution-switch table. If output limits apply, split it into clearly labelled PART 1 and PART 2. Do not truncate, summarize away tables, or use ellipses.

Correct and explain these inconsistencies:

- The allowlist heading says 14 variables, but only 13 are listed.
- The report says “up to 27” variables while 13 inherited plus 13 generated equals 26.
- Identify the exact authoritative counts from source.

D. STATIC SECURITY FINDINGS

Without editing code, provide file-and-line evidence for:

1. The exact spawn call.
2. Whether `vitest` is a bare command.
3. Whether executable resolution depends on inherited PATH.
4. Whether a malicious parent PATH could select an unintended executable.
5. Whether HOME is inherited.
6. Every credential-bearing location potentially exposed through HOME.
7. Whether TMPDIR, TMP and TEMP are inherited/shared.
8. Risks from shared temporary paths, including collisions, symlinks, sockets and residual files.
9. Whether XDG_CONFIG_HOME, XDG_CACHE_HOME and XDG_DATA_HOME are isolated.
10. Whether the child has any way to access external services without environment credentials, including hard-coded URLs or default client configuration.

Required current labels:

CHILD_EXECUTABLE_RESOLUTION: UNSAFE
HOME_FILESYSTEM_ISOLATION: UNSAFE
TEMP_FILESYSTEM_ISOLATION: UNPROVEN
EXTERNAL_SIDE_EFFECT_ISOLATION: UNPROVEN

Do not mark these safe without implemented and tested controls.

E. EXTERNAL-SERVICE MOCK CLAIM

The child sets `TEST_EXTERNAL_SERVICES_MOCKED=true`.

Prove whether this represents an enforced technical condition or only an asserted environment value.

Report:

1. What concrete mechanism blocks/mocks fetch, HTTP, HTTPS, sockets, DNS and provider SDKs.
2. Which setup file installs that mechanism.
3. Whether it runs before application imports.
4. Which providers are covered:
   - Kite
   - Telegram
   - INDstocks/IndianAPI
   - Upstox
   - Apify
   - email providers
   - webhook endpoints
5. Whether hard-coded URLs are blocked.
6. Whether localhost/test-database access is separately allowed.

If no enforcement mechanism exists, label:

TEST_EXTERNAL_SERVICES_MOCKED_ASSERTION: DECLARATIVE_ONLY

Do not claim external-service isolation merely because credentials and proxy variables are absent.

F. DATABASE ISOLATION TRUTH TABLE

Statically document every current preflight predicate:

- TEST_DATABASE_URL presence and parsing;
- permitted protocols;
- hostname rules;
- port rules;
- database-name/run-ID binding;
- username/role restrictions;
- query-parameter restrictions;
- comparison with operational DATABASE_URL;
- production host/database rejection;
- TEST_RUN_ID validation;
- explicit isolation acknowledgement;
- external-service acknowledgement.

For each predicate give:

- file and line;
- accepted example;
- rejected example;
- failure reason code;
- remaining bypass or limitation.

Do not connect to a database.

Until a separately provisioned disposable database is verified by immutable server/database/role identity, the maximum permitted label is:

DB_TEST_ISOLATION: STATIC_READY_RUNTIME_UNVERIFIED

G. TEST EVIDENCE

Report the earlier verification without rerunning it:

1. Exact commands.
2. Start/end timestamp and duration, if recorded.
3. Exit codes.
4. Test files collected.
5. Passed, failed, skipped and timed-out counts separately.
6. Confirmation that only the positive unit allowlist ran.
7. Confirmation that no application test, database test or external integration test ran.
8. Typecheck command and exit code.
9. Any warnings or stderr.
10. Whether the 81 tests prove runtime isolation or only pure helper behaviour.

The correct distinction must be explicit:

- PURE_HELPER_TESTS: PASS or FAIL
- RUNTIME_DB_ISOLATION: NOT_EXECUTED
- EXTERNAL_NETWORK_ISOLATION: NOT_EXECUTED

H. PROPOSED CORRECTION — PLAN ONLY, NO EDITS

Provide a minimal proposed file diff for the next authorized correction. It must include:

1. Resolve a trusted local Vitest CLI to an absolute canonical path.
2. Spawn the absolute trusted Node executable with that absolute CLI path.
3. Remove PATH from the child environment.
4. Create a fresh isolated HOME for each test run.
5. Isolate XDG config/cache/data directories.
6. Create isolated TMPDIR/TMP/TEMP paths for the same run.
7. Clean only the generated run directory after the child exits, using canonical-prefix validation.
8. Do not inherit CI, locale, timezone or colour variables blindly; set only deterministic values actually required.
9. Replace declarative external-service mocking claims with an enforced fail-closed mechanism, or downgrade the label honestly.
10. Add pure tests proving:
    - malicious PATH is ignored;
    - the executable is absolute and trusted;
    - parent HOME is absent;
    - child HOME and temp paths are isolated;
    - XDG paths are isolated;
    - unknown and future secrets remain absent;
    - cleanup cannot target a path outside the generated test root;
    - no production switch can become enabled.

Do not implement this plan yet.

I. FINAL STOP-GATE TABLE

End with this exact table and one evidence-backed value per row:

- DEPLOYMENT_EVENT
- GIT_TREE_INTEGRITY
- CHILD_ENV_POLICY
- CHILD_EXECUTABLE_RESOLUTION
- HOME_FILESYSTEM_ISOLATION
- TEMP_FILESYSTEM_ISOLATION
- EXTERNAL_SIDE_EFFECT_ISOLATION
- DATABASE_ISOLATION_STATIC
- DATABASE_ISOLATION_RUNTIME
- PURE_HELPER_TESTS
- APPLICATION_TESTS
- PRODUCTION_DB_TOUCHED
- EXTERNAL_SERVICE_CALLED
- WORKFLOW_RESTARTED
- P0_1_ACCEPTANCE

P0_1_ACCEPTANCE must remain NOT_ACCEPTED unless every runtime-relevant blocker has actual evidence. Planning language and passing pure helper tests are not runtime proof.

Stop after the report and wait for owner review.